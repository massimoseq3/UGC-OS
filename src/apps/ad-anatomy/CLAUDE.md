# Ad Analyzer — `ad-anatomy/`

Create section of UGC OS. This file loads when Claude works with files under `src/apps/ad-anatomy/`; the app-wide rules stay in the root [CLAUDE.md](../../../CLAUDE.md).

## Job

An ad VIDEO — dropped in, handed over from Outliers, or fetched from a pasted link — → Breakdown (scorecard + hook / angle / structure) + transcript + Scenes. Images are refused with a sentence saying so (`adFileRejection`); the drop zone has taken video only since the first commit, and the prompt's "video/image" wording and `mediaKind: 'image'` are not a working image path (nothing prices a still, and the scene contract chunks by seconds). The Breakdown card carries **no action row** — it used to ship a fourth field, `CreativeBreakdown.stylePrompt`, a product-agnostic writing brief saved to the Script Bank as `kind: 'style'`; **cut July 2026** because stripping every concrete detail (the thing that made it reusable) left generic DTC platitudes whichever ad went in. Bank rows already saved still render (`BankList`'s `kind === 'style'` branch stays); nothing produces new ones. **All three panels share one header band, and the read's is OUTSIDE its scroller.** The section-jump toggle sits in an `h-[57px]` row that is a SIBLING of the scroll port rather than `sticky top-0` inside it (the app-wide rule in the root CLAUDE.md): it was pinned at the top from the first pixel and never scrolled away, so sticky bought nothing but the chance to come loose from the edge. It stays opaque (`bg-surface-0`, no `backdrop-blur`) — the column has no background of its own, so a translucent bar let the cards scroll visibly through it. The media column carries the **filename pill** in a band of its own at the same height, which is why: the pill used to sit at the FOOT of that column, so the hairline the History rail and the toggle bar already draw stopped two thirds of the way across the screen. The jump targets take `scroll-mt-5` on a desktop, since nothing overlaps the top of that scroller — but **`max-md:scroll-mt-[73px]` on a phone**, where the whole results pane is the scroller and that same bar IS `sticky top-0` inside it (the one case the app-wide rule keeps `sticky` for). A bare `block: 'start'` there put each section's own heading squarely behind the bar, so you tapped Transcript and arrived 57px into the transcript with nothing on screen saying where you were; 73px is the bar plus 16px. The jump also calls **`suspendChromeAutoHide()`** — see the root file: without it the dock hid mid-animation, relayouted the pane under the browser's own smooth scroll, and the jump landed 670px past or not at all. **`CardHeader` is a `grid-cols-[1fr_auto_1fr]`**, not a centred title with an absolutely-positioned action: absolute takes no layout space, so at 375px the Scenes card's old long title ran straight under "Copy All" — the same failure `SectionCard`'s header was rebuilt to avoid. **Its glyph is monochrome** (`text-ink-400`, September 2026, Massimo's call — the second time: the glyphs were amber Breakdown / red Transcript / fuchsia Scenes, went monochrome for a day, came back to keep fuchsia Scenes paired with Scripts' Scenes badge, and went monochrome again on the reasoning that three hues on three headings read as decoration). Ask before bringing the colour back a third time. The Breakdown scorecard is a number over a bar per score, in the analyzer red (`scoreValue`); the model scores to one decimal since the same change, and older rows print as "8.0". That header's own button, `MiniButton` (Save style, Copy) and each scene's Copy are all **glyph-only at every width** (September 2026, Massimo's call — the bookmark and copy glyphs already say it), keeping their wording as the tooltip and the accessible name: these sit on a heading's line inside a card already inset twice, and a label there competes with the heading for a line neither fits on. The Visual Style / Voice / scene headings are **centred 13px Title Case over a card-width hairline** (`titleCaseLabel` from `utils/`, shared with Scripts; they were 10–11px all-caps labels). The upload screen and the analyzing screen are deliberately spare — headline + drop zone, then title / the ad playing under the scanning sweep with a **sound toggle** on it (autoplay has to start muted, but the member is watching their own ad). A rotating "Did you know" ad-fact box under the sweep was cut; see git history. The upload screen also takes a **pasted link** (`AdLinkField` in `UploadView.tsx`, via Outliers' `parseAdLink` / `fetchAdFromLink` — see Outliers' CLAUDE.md): with a ScrapeCreators key it spends 1 credit fetching the video into the STAGED list, so the price still shows and nothing is analyzed until Analyze is pressed; without a key its button reads **Connect Key** and opens Outliers' `ConnectScrapeCreators`; while Outliers is switched off the field isn't rendered at all, since the key and its Settings field go with Outliers. **A settled analysis announces itself** (`announceWhenSettled` / `setViewedAnalysis` in `services/analysisQueue.ts`): the analyzing screen tells the member they can switch tools, so a run the Ad Analyzer started (drop zone, Outliers handoff, Retry, a resume on open) toasts its title with **Open** (the `openAnalysis` payload) or its humanized failure — unless the member is looking at that analysis in this app right now. Flow's blocks share the queue and report in their own Run View, so they don't toast; a key or credit failure keeps the toast's own Connect Key / Add Credits button instead of Open. What the spare screens DO have to say is **how long this takes** — "Please wait — this can take a couple of minutes", once under the Analyze button (before they commit) and once under the sweep. A whole-video read is one long call with no progress to stream, so an unstated wait reads as a hung page, and members were reloading out of it. The analyzing line deliberately stops at "it keeps running if you switch tools" and does NOT claim it survives a refresh: it usually does, but a reload before kie hands back a taskId can't be resumed, and a promise broken once is worse than one never made. A **`PreparingPane`** covers the gap between the Analyze click and the history row landing — an IndexedDB write of the whole clip, during which there was no `selected` row and the screen fell back to a bare upload zone, so the click looked ignored. **Nothing that dies loses the member's upload.** `uploadedRef` now survives both a failure and a refresh-interrupt (it used to be dropped and the asset deleted), so `ErrorPane` leads with **Retry Analysis** — `retryAnalysis` reloads the blob, rebuilds the File and re-enqueues — and falls back to "Upload Another" only when the source is genuinely gone. The mount-time TTL sweep covers error rows on the same 14-day rule as complete ones, so retained sources stay bounded, and deleting the row purges the asset outright. **The ad sits on the RIGHT and the read on the left** (August 2026): the History rail is already down the left edge, so a media column beside it put two narrow columns between the member and the thing they came to read, and the breakdown — the widest, longest content on the screen — started a third of the way in. The media column stays FIRST in the DOM and moves with `md:order-2`, because the two layouts want different reading orders: on a phone the ad is the anchor you land on and the breakdown scrolls under it, on a desktop the breakdown is what's being read and the ad belongs beside it. **The source player is also the frame grabber.** While a row still holds its `uploadedRef`, the left column plays the real ad, and **Download This Frame** under it — at the `px-4 py-2.5` / 16px-icon weight of the `ScriptActionRow` buttons, since it is the media column's only action and a chip-sized one read as an afterthought — hands over the frame the playhead is parked on — full source resolution, PNG, named `<file>-frame-00m01s40.png` — because the alternative members were reaching for is a cropped screenshot of the app window. `captureFrameFromElement` (`utils/videoFrames.ts`) reads the LIVE element rather than loading and seeking a hidden one the way `extractVideoFrame` does: no second decode, and what lands on disk is exactly the frame on screen, which is the whole promise when the member picked the moment themselves. It pauses the player first, or the grab races the click for whichever frame happened to be up. The native controls stay the only scrubber — a second timeline under a third-width player would be a worse one than the browser's. It renders only for `mediaKind === 'video'`, a flag `ResultsView` now takes for the other half of the same problem: an image ad was handed to a `<video>` element regardless and drew an empty player. **Both players carry the saved first frame as their `poster`, and they have to keep it.** Safari's default `preload` is `metadata`: it reads the clip's header off the blob — the element sizes itself correctly, so nothing is wrong with the source — and then paints NOTHING until playback actually starts, so an unplayed `<video>` is a black rectangle with a play button on it. Chrome defaults to `preload="auto"` and paints frame one, which is why this was invisible to anyone testing in Chromium (September 2026). On the results column it was worse than a black box: the branch under the player renders that same still while the video blob is still resolving, so the member watched their ad appear and then be replaced by black a moment later — reported as "it pops up for a second and then it just disappears". On the analyzing screen the same gap is covered by `autoPlay`, right up until Safari declines it (a per-site Auto-Play setting alone is enough), which is why that pane also asks for `play()` once the source resolves and treats a refusal as normal. `preload="auto"` on the results player is a hint Safari downgrades freely — the poster is the half that actually guarantees a picture. **The ad is UPLOADED to kie's file host and referenced by URL — it never rides inline** (`buildMessages`, August 2026), and this has now been settled in both directions. kie's gateway refuses an inline `data:` clip of any real size with a 400 that names the fix itself: *"Inline data URL is too large. Upload the file and pass an HTTP(S) URL instead."* That arrived on a **~5.4MB** ad — comfortably under the budget below, so nothing compressed it and nothing warned the member — which means the ceiling is the GATEWAY's, not the model's, and **no chat-model swap moves it** — this failure was read as Gemini 3.6 Flash being unreliable and very nearly bought a model swap that would have changed nothing. Hosted URLs were tried once before and reverted the same day (PR #91 → #92, May 2026), but not because the upload failed — the revert message says createTask succeeded and `pollAnalysisTask` couldn't read the result back, through a narrow local envelope reader that was replaced in August 2026 by the shared `extractChatTaskText` (PR #479). That blocker is gone; the 400 is what makes inline untenable either way. `ensureHostedUrl` memoises by content hash, so a Retry costs no second upload, and hosted files outlive any analysis (~3 days). **An oversized ad is still re-encoded in the browser rather than refused** (`utils/compressVideo.ts`), because the upload POST carries base64 too and 4/3 inflation still applies to it. `VIDEO_UPLOAD_BUDGET_BYTES` (12MB, in `analyzeAd.ts`) is the one number that governs it — it bounds the UPLOAD now, not a request body, and it is deliberately unchanged from the inline era: moving the transport and the budget together would be two variables at once. It is very likely raisable now that nothing rides in the request; establish the file host's real cap on a big ad first. Anything under it is sent untouched, anything over is re-encoded to fit: anything under it is sent untouched, anything over is re-encoded to fit — long edge 720 (480 when the budget forces the bitrate under 700kbps), and **the bitrate derived from the budget and the duration** rather than picked and hoped over. **The first pass is aimed to LAND, because a retry is a wait the member watches.** Three settings do that work and they are all deliberately generous. `BUDGET_HEADROOM` is **0.55** — the first aim is a little over half the budget, so the encoder can miss by 1.8x and still fit; the cost is a little quality on a clip only a model ever reads, against a second realtime pass the member sits through. `TARGET_FPS` is **12**: the model samples the clip at roughly one frame a second, so everything above a few fps is bits spent on motion nothing looks at, and frame rate is the single biggest lever on what the encoder wants. `MAX_VIDEO_BITRATE` (**2.5Mbps**) is what bounds a SHORT ad, where the budget spread over a few seconds buys a bitrate no encoder needs and the aim is irrelevant. **And the recording watches its own size while it runs.** `recorder.start(1000)` hands over a chunk a second, so the run projects its finished size from what it has already written, and thins the frame clock (12 → 6 → 4fps, `clock.retime`) when that projection heads past the budget. It is the half of the file that does not depend on the encoder cooperating at all — fewer frames is fewer bits whatever its rate control is doing — and it is a backstop under the conservative aim, not a substitute for one, since bits already written can't be taken back. **A bitrate is a request, not a contract, so the pass MEASURES and re-aims** (September 2026): `videoBitsPerSecond` is a number the encoder is free to miss, and on a high-motion ad it misses high — a single pass came back **13.3MB** against the 12MB budget and the queue could only report that as a failure to a member who had already sat through the whole re-encode. So an overshoot is re-aimed from the size the encoder ACTUALLY produced and run again, up to `MAX_PASSES` (3), landing at `RETRY_HEADROOM` (90%) of the budget rather than exactly on it — one sample of a variable-bitrate encoder is not precise enough to bet a third realtime wait on. **The correction is computed against the bitrate that was asked for, not against the aim**: `MAX_VIDEO_BITRATE` can clamp a short ad's request well below the aim, and correcting the uncapped number hands the next pass the identical capped bitrate and spends a whole realtime run re-recording the same file. It stops early when the bitrate is already on `MIN_VIDEO_BITRATE` or a pass shaved under 5% off the last — both mean the file will not fit at this length, which is the one case the "still XMB after compressing" error is now for. Every extra pass is another wait as long as the ad, so `onPass` reports it: the queue stamps `compressPass` on the history row from the second pass on and the analyzing screen says the re-encode is running again, rather than letting the runtime it quoted quietly come and go. Four things about the pass itself are load-bearing. It is **realtime** (MediaRecorder encodes at playback speed, so a 40s ad costs ~40s) — which is why the staged pill carries a **Compress First** chip before the member commits and the analyzing screen says *"Compressing the ad’s file size"* rather than sitting on the sweep for an extra minute unexplained. The container is **unqualified `video/mp4`**: `MediaRecorder.isTypeSupported` answers true for `video/mp4;codecs=avc1.42E01E,mp4a.40.2` in Chrome and the encoder then dies with a bare `EncodingError: Internal Error`, so let the browser pick its own profile. The frame clock runs on a **worker**, because a hidden tab throttles main-thread timers to ~1/s while the video element and the recorder keep going — measured at 4 ticks against a worker's 86 over 3s — which produced a full-audio clip over a 1fps slideshow the moment someone switched browser tabs (0.32MB against 3.94MB for the same 20s source). And the element is **never muted**: Chromium applies effective volume before the audio reaches a `MediaElementAudioSourceNode`, so muting silences the Web Audio capture too, and a silent re-encode does not fail — it comes back as a confident analysis of an ad nobody speaks in. An inline `AnalyserNode` (source → analyser → destination, since a `MediaStreamAudioDestinationNode` has no output to tap) watches the level and the run is failed if it recorded pure silence out of a file whose decoder reported audio. Its errors are `FriendlyError`s (`utils/friendlyError.ts`), which `humanizeError` hands back verbatim — they already name the file's size and what to do about it, and the rule table could only flatten that into something vaguer. **`startAnalysisTask` no longer falls back to streaming on a size rejection**: the fallback exists for a createTask route that isn't available for chat, and re-sending the identical oversized body down a second transport just fails again after another full upload. **An analysis that comes back CUT OFF says so, and there are two tests because neither catches the other** (September 2026). The transport's stop reason is authoritative when it fires — `TruncatedResponseError` on the streaming path, `chatTaskHitTokenLimit` on the jobs one, now read here as well as in B-Roll — but a stream that is simply cut carries no stop reason at all: the connection drops, or kie's gateway ends the SSE without a final chunk, and the partial answer arrives looking clean. It then died at `JSON.parse` with `JSON Parse error: Unterminated string`, a message `humanizeError`'s rule table cannot recognise, so the member got "Analysis failed." and 400 characters of raw JSON in the console — reported from Safari, September 2026. So `parseAnalysisJson` also asks the structural question: **did the JSON document ever close?** `jsonDocumentIsTruncated` walks the text tracking string/escape state and bracket depth, and anything still open at the end is an answer that stopped mid-write. It deliberately answers "does it terminate", NOT "is it valid" — a model that closed its object and then wrote a sentence after it returns to depth zero and keeps the developer-detail error it has always had, which is the one that belongs on a genuinely malformed answer. Both paths throw the same `INCOMPLETE_ANALYSIS` sentence, because `humanizeError`'s own token-limit rule is written for Scripts and B-Roll and advises "a shorter script or fewer scenes" — neither of which is a thing a member analyzing an ad has. **It does not try to salvage the partial**, and it must not: the tolerant-parser lesson from B-Roll's storyboard is that a half-answer rendered as a whole one is worse than an error. Asking for the rest the way `storyboardCompletion.ts` does is the open option if this turns out to be a ceiling rather than a cut stream — the evidence so far cannot tell the two apart, which is why the copy names Retry first and a shorter cut second.

**What counts as an ad is `services/adUpload.ts`** — the formats, the 50MB cap and Compress First — shared with Flow, whose Ad Analyzer block takes a member's own ad dropped in (see Flow's CLAUDE.md). Change it there and both drop zones move together. `adFileProblem`'s short strings are a contract — Flow's `readAd` compares against `'Unsupported format'` — so the sentence a member reads beside a refused file is `adFileRejection`.

**The analysis is ONE chat call** (`analyzeAd.ts`): the video inline as the only media part, returning scorecard + breakdown + transcript + scene prompts in one JSON object. It is one of **two surfaces on `CHAT_MODEL_STRONG`** (Gemini 3.8 Flash since September 2026, Gemini 3.6 Flash before it — the Bank's product auto-fill is the other) and the only one at **MEDIUM** reasoning effort (August 2026 — it ran on the app-wide default at low effort before): everything it returns is read by a person and shot against, so a misread style family or a hedged scene prompt costs a re-shoot rather than a retry, which is the case that tier is kept for. Both knobs live at the top of `analyzeAd.ts` and BOTH transports read them — `kieChatCompletions` defaults to low effort, so the streaming fallback has to be handed `REASONING_EFFORT` explicitly or it quietly analyzes at a different effort from the task path. `analysisCost.ts` must track `CHAT_MODEL_ID` too, or the Analyze button quotes one model's rate for another model's call; its `REASONING_TOKENS` allowance is there because thinking is billed as output whether or not `include_thoughts` shows it. It polls on `CHAT_POLL_ATTEMPTS`, not the 5-minute default a whole-video read outlives, and `taskId` on the history row is what a refresh resumes. **In practice the STREAMING fallback is what runs today** (verified 2026-09-06): kie's jobs route has no chat route for the Gemini rows, so `createTask` 422s with "The model is empty" and the queue falls through — the analysis works, it just isn't resumable, and the console says which path ran. This is unchanged from Gemini 3.6 Flash, which 422s the same way; do NOT reach for the `-openai` variant slug to make it resolve, which registers a task that then fails at the poll where no fallback remains (see the registry entry). A **two-pass split is retired** (shipped #261, reverted July 2026): perception (video + ~20 client-side cut keyframes → transcript + per-cut shot log + dossiers) then text-only synthesis. It was written for more shot-level detail and every analysis came back rejected as invalid; budgeting the inline payload and buying a video-only retry (#373) didn't fix it. Its `PerceptionResult` / `SynthesisResult` types and `utils/extractKeyframes.ts` are deleted (see git history); `AdAnatomyHistoryItem.perception` stays as a legacy field nothing writes. If it's ever revisited, reintroduce ONE variable at a time — extra media parts, reasoning effort, and the second call are three separate suspects. **The Scenes card leads with two master blocks** (`MasterVisualStyle` / `MasterVoiceProfile` on `ReverseEngineeredPrompt`; both optional — legacy rows predate them, and a silent ad has no voice). Every scene prompt stays self-contained because each one is fired as its own clip; the masters exist for the two things that must NOT drift between clips. **Visual Style** classifies the ad against B-Roll's own picker list (`styleFamilyMenu()` off `CONTINUOUS_STYLES`, so a style added there is immediately one the analyzer can name) plus `'other'`, badges `liveAction` — the "UGC realism or AI animation?" question answered on its own — and carries a brief written to `STYLE_BRIEF_SPEC`, the same five axes `analyzeStyleReferences` distils, so **Save style** drops it straight into the `styles` bank and the member can render their OWN ad in the winner's look (the button tracks the saved BRIEF, not a boolean — the component survives a History switch, and a flag left the next ad reading "Saved"). An unrecognised id normalises to `'other'` rather than through `getContinuousStyle`, whose fallback would label an unknown look "3D Animated". **Voice** is `VOICE_PROFILE_SPEC` (shared with Scripts via `utils/voiceProfile.ts`) plus `delivery` — on-camera sync vs. voiceover over b-roll vs. an interviewer, which is what recreations get wrong most. Both ride the top of Copy All and of the Scripts handoff in the `=== MASTER … ===` shape Scripts already emits; `detectSceneBlueprint` matches scene headers anywhere in a source, so the preamble is safe — and **every scene carries its `--- Scene N ---` header, a one-scene ad included** (August 2026). `joinScenes` used to hand a lone scene over bare, on the reasoning that one prompt needs no divider; but the header is what makes the text a BLUEPRINT rather than prose, so Scripts read it as a plain transcript and remixed it into three spoken script variations — no scene rewrite, and no voice profile, which is the shape members reported. A lone scene is the COMMON case, not an edge one: the chunking rule returns a single scene for any ad of 15 seconds or less. The bare prompt is still one click away on the scene's own Copy button, which hands over `scene.prompt` verbatim; the card header's copy is the whole set either way, and reads **Copy Scenes**. **The scene prompts are written for reproduction, not description**: every shot accounts for camera position as geometry, movement, setting, action, lighting, the dialogue heard over it and the on-screen text quoted verbatim; hedges and generics ("a woman", "appears to be") are banned outright, a hard-to-see visual detail is committed to rather than hedged — but a brand name, caption, price or spoken word is never invented — and each scene repeats the full character and product description, since "the same woman as before" is a broken prompt when the scene is fired alone. **A scene prompt is READ as its beats** (`utils/scenePrompt.ts`, rendered by `SceneCard`): the card splits it on the `[0:00–0:03]` markers the prompt contract already asks for, puts each marker in red on its own line — followed by how long that beat RUNS (`rangeDurationLabel` in `utils/timecode.ts`, shared with Scripts' scene headers, which show the same ranges off the same blueprints), since the range is where the beat sits and the duration is what it gets reproduced at — and lifts the quoted verbatim lines out of the prose into their own boxes — the shape Scripts gives a scene, since both are a shot's direction wrapped around the words that get said. Two quote kinds, because one is heard and the other is read: a spoken line takes the scene accent, transcribed on-screen text stays neutral chrome, and each copies on its own. It rewrites nothing — **Copy still hands over `scene.prompt` verbatim**, which is the artifact; this is a reading of it, and one long paragraph of camera geometry with the dialogue buried mid-clause was the thing nobody could scan. It deliberately does NOT reuse Scripts' `splitSpokenLines`: that parser holds the tighter contract (`[CHARACTER] says: "…"`, always double quotes, always a colon), while the analyzer transcribes what it heard — single quotes as often as double, `says,` as often as `says:`, a cue that runs on before it hands over ("The voiceover continues over the shot: …"), and quoted on-screen text that is not dialogue at all; bending one parser to serve both would loosen the one holding the tighter contract. Three guards earn their keep: an **unattributed** quote stays in the prose (a scare quote is direction, and promoting it cuts the sentence in half), a **single** quote only closes on a mark not followed by a letter (the prose is full of "the woman's knee"), and a lead that doesn't resolve to something short and name-like renders the box **unlabelled** rather than printing half a clause as a speaker. A prompt with no markers — a single-shot scene, a still — comes back as one untimed beat, which renders as the plain paragraph it always did. The Transcript and Scenes cards each carry their Scripts handoff ONCE, at the foot in a two-button row (`ScriptActionRow`): **Save to Script Bank** + **"Remix For Your Product"** / **"Rewrite Scenes For Your Product"** — both handing their text to Scripts' Remix box (no product rides the handoff; the member picks one in Scripts). **Each saves once**: `useIsInScriptBank` reads whether that exact text is already a Script Bank row, so Save turns into an emerald **Saved to Script Bank** and Send files a row only when none exists — Send used to call `addScript` on every press, and Save then Send left two identical rows. Read from the bank, not a flag, because the section stays mounted across a History switch (the Visual Style block's saved-brief reason) and a row deleted in the Bank turns the button back into Save. A Scenes save is titled `<ad> · Scenes`. They carried it twice until September 2026, with a `HeaderPill` ("Remix For Your Product" / "Clone This Ad") on each title's line as well; the pills came out on Massimo's call — the same action twice per card is one too many, and the header keeps only Copy. Rewriting the blueprint shot-for-shot for the member's product is honest here where the B-Roll button below wasn't. Their label colour is `text-scripts-text`, never `text-scripts-400` — Scripts' accent is a dark navy (`#24365A`), so a 400 label on its own tint measures ~1.6:1 and reads as a disabled button (still true in light mode, where 400 mixes from the brand navy toward black; dark mode's 400 is now a lift of that block's own 500, but it exists as the hover step for a filled button, not as a text tint). That token is the app-wide rule for **any** text or glyph sitting on a scripts tint. **A third button, "Clone this with my product", is removed** (July 2026): it read as "recreate this ad" but actually dropped the member on the B-Roll page. Its whole pipeline is still wired and unreferenced — `services/adBlueprint.ts` → `AdBlueprintPayload`, targetField `adBlueprint`, consumed by B-Roll as its only scene-staging input — so restoring it is re-adding the button under a label that says where it goes. What it sent is worth keeping in mind if it comes back: **staging, not prompts** — the ad's transcript becomes B-Roll's script and its beat map + shot craft ride `BrollInput.sceneStaging`, the same seam a Script Style format uses, so B-Roll writes fresh prompts against the member's OWN product and character refs. The analyzed scene prompts deliberately stay behind — they name the original creator's face and the original brand's packaging (the recreation doctrine requires it), so shipping them alongside the member's references would put two people and two products in frame and render a competitor's label as the member's product.
