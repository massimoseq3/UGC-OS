# B-Roll — `broll-studio/`

Create section of UGC OS. This file loads when Claude works with files under `src/apps/broll-studio/`; the app-wide rules stay in the root [CLAUDE.md](../../../CLAUDE.md).

## Job

**Continuous is opt-in and ships OFF** (`stores/appVisibilityStore.ts`'s `OPTIONAL_FEATURES`, switched in Settings → **Experimental**; per-browser, never cloud-synced). It gates on ONE derived value — `BrollStudio` reads the flag and hands everything downstream `mode = enabled ? storedMode : 'line'`, so the toggle, the right panel, Generate and the import modal are all covered by that line and nothing else in the app has to ask. Two surfaces do read the flag itself: `InputPanel` drops the mode toggle **and its whole 57px header bar** (Massimo's call — that bar exists only to hold the toggle, so with one mode left an empty band with a rule across it was 57px spent lining up with a header that has something in it), and `RightPanel` filters Continuous rows out of History and drops the "Line by Line" qualifier from the Storyboard tab. Nothing is written, migrated or deleted: the stored mode, the keyframe chain, its selections and its history rows are all left as they are, so switching it back on reopens the session exactly where it was — the same promise Outliers makes one level up, and the same reason History hides a Continuous row rather than orphaning it (there is no mode left to open one in, exactly like `isRetiredOneShotRow`).

**The scene header CENTRES on a phone** (August 2026). Its desktop shape is a masthead — take number, hairline rule, then the line running away to the right, batch buttons on the far edge — which needs a column wide enough for the rule to divide something. A phone doesn't have one, so the number sits over the line, the rule goes, the quote is centred, and the two per-scene batch buttons centre under it. What survives is the take number and the words, which is what the header is for. **The line itself is upright Instrument Serif, not italic** (September 2026, Massimo's call) — only the numeral is italic, which is also what `ContinuousView` had always done, so the two deliveries now set the same sentence the same way. It was swapped to the app's own sans for a day in the same month and put straight back (Massimo's call, both directions): the serif line is the masthead, and the four places it is drawn — `ScenesView`, `ContinuousView` and both detail modals — have to move together or one delivery sets the sentence differently from the other. **The `LINE N` chip is gone at every width** (August 2026, Massimo's call): it printed the scene's number two inches from the 48px italic numeral that is the masthead's own left half. A phone had already dropped it for that reason, and a repeat is a repeat at any width. The line editor under it carries **no explainer** either: three lines saying that saving is free, instant and doesn't touch the shots is what saving a line has always done, and reading it every time you fix a typo is clutter rather than reassurance.

**Every card workspace can fire up to four TAKES of its own prompt** (`BatchCountStepper` in the per-line card modal's Image / Video / Animate footers, Continuous' frame modal and Continuous' clip modal; one shared persisted count, `ai-ugc-lab:broll:card-takes`). The axis matters: a scene's three **variations** and a frame's three **concepts** are three different IDEAS for the beat, written by the storyboard call — three **takes** are three rolls of the same one, which is what a card's own gallery exists to choose between and the one thing these modals could not ask for. It needs no new machinery because `CardState` already carries parallel `inFlightImages` / `inFlightVideos` queues keyed per generation, so a batch is N pushes onto them and each take resumes across a refresh on its own. **Not on the header batches** (`Generate all images` / `Animate all stills` / `Generate all videos`): those already multiply by scene × option, so a count there would turn a twelve-clip run into thirty-six. Video takes cost far more than image takes, which is why the Generate button beside the stepper prices the whole run.

**Clip length follows the SPOKEN line, not a setting — and only under Dialogue Clips** (`services/clipDuration.ts`, August 2026). A card whose character speaks its line to camera is as long as the words take to say. It was a flat 5s on every card, which is wrong in both directions and was reported as both: a twenty-word line crammed into 5s comes back with the character gabbling, and a five-word hook stretched over the same 5s comes back slow and dead. **The estimate shipped applying to every card in both deliveries and was pulled back to the dialogue cards within the day.** A B-Roll Clips card has no words in it — it's cutaway footage a voiceover is laid over in the edit — so nothing about its length follows from the line it was written for, and a word count there is a number invented for a clip that isn't saying them; those cards keep the flat 5s (snapped down) they always had, their duration chip has no Auto row, and a silent run's batch dialog pins one length exactly as it did before. `speaksItsLine` is the one test and it's `tag === 'DIALOGUE'` — the same predicate the voice profile and the anchor chain already go by, so a hand-added option (tagged ACTION, silent everywhere else in the app) is silent here too. `spokenClipSeconds` is `words / 2.4 + 0.5s` of lead-in/tail, floored at 4s and capped at 15s (past that, splitting the line in the scene editor beats paying for a 20s clip); `autoClipSeconds` snaps that onto the model's own ladder, to the **NEAREST** rung. **It ran 1–2s long on every card at first, and the fix was to stop rounding up three times over**: a full second of lead/tail, a `Math.ceil` on top of it, then a snap UP. Only one round-up survives — the snap, which is unavoidable — and it's a nearest now, because a ladder is coarse (…5, 6, 8, 10, 12…) and an estimate overshooting a rung by a third of a second was buying a whole extra rung. There's no truncation to protect against anyway: the video model FITS the speech to the clip it's given, so a rung short reads as a brisker delivery (the UGC register) while a rung long is dead air at the end of every cut. Measured on Grok's ladder that's 1–2s off a typical line and never longer than before. (The app-wide `snapVideoDuration` still rounds DOWN, which is right for a default nobody chose; `snapVideoDurationUp` survives for a length the member PINNED, where their number must not shrink under them.) Deliberately a word count rather than a number the storyboard model answers with: it's free, deterministic and debuggable, it costs no prompt surface and no parser, and it's the same ~2.4 words/sec pace Scripts writes its word budgets against, so a script written for a beat and the clip rendered for it agree by construction. **Continuous has estimated its scene lengths this way since it shipped** — `sceneDuration` is now that shared estimator with its own 3s floor (quick punchy beats), because the two modes cut the same script into the same beats and had no business disagreeing about how long a line takes to say. A dialogue card is **Auto** until the member picks a number in its duration chip (`CardState.cardVideoDurationAuto`): `cardClipSeconds(card, line, model, { spoken })` is the single place that decision lives, so the chip's trigger ("Auto · 8s"), the modal's credit estimates, the batch dialog's per-card pricing and the generation itself can't drift — and `spoken` is a required argument rather than a defaulted one, since a call site that forgot it would quietly put Auto back on the silent cards. Auto survives a model swap — the estimate re-derives onto the new ladder rather than keeping the old model's number — while a pinned length keeps the old snap-down clamp, since that one is the member's. On a silent card the same flag reads as "did anybody pick this number": true means the value in the slot was derived rather than chosen, so `silentClipSeconds` ignores it and returns the 5s default — which is what un-does the day the estimate applied to everything, without touching a length the member really did set. Cards persisted before the flag land on unpicked only when they hold one of the two values the old seeder could produce (5, or the 4 it snapped to on a ladder that skips 5): a stored value is a real pick everywhere else in the app, but here every card stored one whether or not anyone chose it, so those two ARE the unpicked case.

**A benefit is a picture, a spec is not — and the product context is labelled so it reads that way** (`buildProductContext` in `services/productAngles.ts`, August 2026). Massimo asked for the Scripts benefits-over-features fix to be carried across, and the copywriting half of it has nothing to attach to here: **this app never writes the copy.** The script arrives already written and rule 1 copies every `<LINE>` verbatim ("use the script's exact words in the script's order… never paraphrase, add, or reorder"), Continuous the same. What DID carry across is the cause. The product context was one flat sentence — `Product: … USPs: … Benefits: … Key specs: …` — pasted into Line-by-Line's user prompt with **no framing whatsoever** (Continuous at least said "this is the ADVERTISED product"), which is the same bug Scripts had: unlabelled product material carries no job, and the spec list is the most concrete thing in the prompt. The failure it produces here is VISUAL — hand a model a spec and ask it for a picture and it draws the spec: the claim printed on the wrapper, the nutrition panel held to the lens, the number across the frame. So each field now states what it is FOR in a pipeline whose output is a shot, and the string says out loud that **the attached photo describes the product and this text does not** (rule 8's "the product reference image is the source of truth" had no line connecting it to the spec list sitting in the same prompt). It is built in `productAngles.ts` and used by all five consumers through the one string — both storyboard prompts, `frameBriefBlock`, and the two per-card rewrites — so the labels are the single lever. Empty bank fields are now **dropped** rather than rendered as a labelled blank: only `keySpecs` was ever guarded, so a product saved without USPs shipped the model a literal `USPs: .` The prompt side is the visual translation of the same rule — **A BENEFIT IS A PICTURE, A SPEC IS NOT**, under SHOW-DON'T-TELL in both system instructions: when a line makes a promise, show the promise KEPT inside a life, and when the shot IS the product it is the product being USED or the result it left behind, never presented and never its packaging read like a spec sheet. Two lens definitions named the object ahead of the outcome and were reordered — `PRODUCT` ("the product itself or its visible result") and `PROOF`, whose "visible evidence the claim is real" resolves to the ingredient list when a spec list is in the same prompt — plus self-check 6b, which is what makes the rule survive contact with a full Key Specs field. **None of this is measured**; it is prompt work, so judge it on a real storyboard before tuning further. **September 2026:** the Bank removed its Key specs & facts field outright — the one this string called its weakest material — so `buildProductContext` no longer sends a spec list at all. In its place it sends the brief's **Before / after**, which is two first-person paragraphs of a day before and a day after: already written as moments (a room, a time of day, a thing in someone's hands), which is a shot list in everything but name and the only material here that describes anything a camera could point at. Its label says to stage the concrete DETAIL and never the feeling it is named for. Nothing else from the brief comes across — objections and the rest are words for the script to say, and this file's whole rule is that nothing here becomes words.

**The storyboard call is a JOB, not an awaited fetch** (`services/storyboardRun.ts`, August 2026). Writing the prompts was the one generation in the app that showed nothing anywhere until it landed: every other surface pushes an in-flight tile before the network call — that's what makes History the queue view — while this one left History empty and the panel spinning, and a reload threw away a call kie had already billed for. Both halves are fixed by the same move, and it's the Ad Analyzer's shape (`analysisQueue.ts`) rather than a new one. **The history row is written FIRST**, in `storyboardStatus: 'writing'`, so the session appears in History from the press, reports "Writing storyboard…" under a `GeneratingPulseRing`, counts into the "N sessions rendering" chip, and keeps doing all three while the member works in another app. **The call itself goes through kie's task transport where the picked writer model has a job route** (`createTask` → `recordInfo`, the model named by `chatSlug ?? id`): the taskId is persisted on the row, so opening B-Roll after a reload re-attaches the poll (`resumeStoryboard`) and the storyboard lands in the workspace as if nothing happened. A model with no job route 400s at `createTask` and falls back to streaming — that path is honest but unresumable, and the console says which one ran. **A row nothing can finish is STRANDED, never left pulsing** (`isStoryboardStranded`): on mount, a 'writing' row with no taskId is flipped to `'error'` when it's this browser's own pending run, and only after `STORYBOARD_TTL_MS` (30 min) otherwise — a row synced from another device may still be writing over there. Three rules hold it together: **the workspace is not touched until a storyboard exists** (the old commit discipline — rotating the session up front left a failed call showing the previous scenes stripped of every image), so `adoptStoryboard` is the one place a result lands, in both the live and the resumed case; **the response is parsed against the ROW, never the live panel** (`parseStoryboard` reads `lineDelivery` / `styleId` / `styleBrief` / `continuousModelId` off it), so a storyboard that finishes after a reload — or after the member changed the toggle while waiting — parses exactly as it would have at the moment it fired; and **a failed row keeps its friendly message on the card** and is not clickable in either state, since it holds nothing to restore (`handleSelectHistory` guards on `storyboardStatus`). A missing kie key is checked before the row is written — a failure we can see coming has no business leaving a dead session in History. `buildBrollMessages` / `buildBrollResult` and `buildContinuousMessages` are the split-out halves of what used to be `generateBroll` / `generateContinuous`; the transport lives in one place now, and `extractChatTaskText` (in `utils/kie.ts`) is shared with the Ad Analyzer so a result shape learned by one is read by both.

**A storyboard that stops short of the script asks for the rest** (`services/storyboardCompletion.ts`, September 2026). This is the longest single output in the app by a wide margin — three paragraph-length prompts AND three motion lines for every line of the script, under a prompt that explicitly forbids trimming a detail to keep the answer short — so it is the one call that routinely runs into a model's output ceiling. When it did, nothing noticed: the tag readers in `xmlBlocks` are deliberately tolerant of a missing closing tag, so half a storyboard rendered as a SHORT storyboard, with no error anywhere, and the member fired paid image and video generations against scenes their script never got. Reported as B-Roll "cutting out half way through". **Two tests, because there are two ways to come back short and neither catches the other.** The transport now says when the model hit its ceiling — the streaming one always threw `TruncatedResponseError`, and the jobs one (the one this call actually takes) reads the same stop reason as of `chatTaskHitTokenLimit`. But a model that simply STOPS EARLY reports a clean finish, and that is not hypothetical: Gemini 3.7 Flash was promoted and reverted inside a day over exactly this symptom. So the second test measures the answer against the SCRIPT, using a rule the prompt already enforces — every `<LINE>` is the script's own words, in order — and calls a storyboard short when its lines account for under **85%** of the script's words. The floor is not 1 because the model may drop a connecting word at a split; a run that stopped half way sits near 0.5, nowhere near it, and the script is normalised first (`spokenScript` strips `[stage directions]`, `(beats)` and `HOOK:` labels) so scaffolding the model was never asked to quote can't read as a shortfall. **The answer to both is to ask for the rest, not to error**: the partial goes back as the assistant's own turn, trimmed to its last COMPLETE scene, with one instruction to carry on — which keeps everything a fresh run would lose, most importantly the dialogue anchor take every VAR_1 has to stay inside. Up to `MAX_STORYBOARD_CONTINUATIONS` (3) rounds, stopping the moment a round adds no new scene, and the text kept is always the longest we ever held. A continuation's taskId is deliberately **not** persisted over the first one — a reload mid-continuation would otherwise re-attach to the tail and adopt half a storyboard's second half as the whole thing. **A resumed run is checked but never continued** (its messages went down with the previous page, and rebuilding them from the row would drop the product and character context it was written against), and anything still short after the loop is **adopted with a warning that REPLACES the ready toast** (`STORYBOARD_INCOMPLETE_WARNING`) rather than failed: what came back is real work the member paid for and its scenes are usable, but two toasts where the first says "ready" is how someone reads past the one that matters.

**A failed generation that still holds a kie taskId is an INTERRUPTION, not a failure** (August 2026, `cardDetailParts`' `FailedTile` / `InFlightFailureRow` / `keepInFlight` in `cardState.ts`). kie ran the generation and billed for it; what died was our side of the hand-off — the CDN download stalled, the browser couldn't decode the blob, the Wi-Fi dropped between the render finishing and the file landing. Retry has resumed rather than re-fired since `resumeInFlightVideo` shipped, so it was already free; what was missing is that **nothing said so**, and three things conspired to lose the clip anyway. First, the copy: the browser-side failures (`probeVideoBlob`'s metadata timeout, a rejected blob) carried the word "timed out" and fell through `humanizeError`'s generic timeout rule to *"it's likely still running on kie.ai — check there before generating it again"*, which describes a different failure and reads as "Retry will charge you again". They have their own rule now, the probe budget went 8s → 30s (it is a LOCAL decode, and it was blowing when a whole batch landed at once), and the raw error is `console.error`'d before it's humanized — the friendly copy is all the entry keeps, so without that every one of these looks identical in a bug report. Second, the tile: a resumable entry renders **amber "Not saved"** with "Retry is free — kie.ai already made this one" instead of the red FAILED alarm, and its **Dismiss takes two clicks** (arms into "Discard?", reverts after 3s — the `TileDeleteButton` idiom), because dismissing is the one move that really does throw a paid clip away. A dead entry — one that never reached kie — keeps the red treatment and a one-click Dismiss, since nothing was charged. Third, and the actual data loss: `backfillCardState` swept every in-flight entry at 30 min (images) / 60 min (videos), **including the ones holding a taskId**, so a member who came back the next morning had them deleted on hydrate before any resume could run. kie keeps a result for **3 days**, so `keepInFlight` now gives a resumable entry that long and leaves the short sweep for entries with nothing to go back for. The resume passes in `ScenesView` / `ContinuousView` already walked errored entries (they keep their taskId) and now re-run on `useReconnectTick` as well as on mount — a clip lost to a Wi-Fi blip lands on its own when the connection returns, with `claimTask` stopping the extra pass from double-polling. `online` only, deliberately: a failed attempt re-toasts, and a genuinely dead task would re-report itself on every tab switch.

**A Line-by-Line prompt is ONE FRAME, not a sequence — and the guarantee is appended at fire time, not just asked for** (August 2026, `PROMPT_FORMAT_CORE` in `services/generateBroll.ts` + `services/realism.ts`). One paragraph feeds both the still and the clip it animates into, so a prompt that narrates a CHAIN of beats — the drawer slides open, then the hand sweeps the pile out, then the drawer knocks shut — hands an image model three moments and it draws all three: a strip of stacked panels, a split screen, a before-and-after, one impossible frame with three pairs of hands in it. Reported as "weird results", and it was never the image model's fault — the **reference example in the system prompt was itself written as a sequence**, and a model copies the example over the rules. So all three example prompts are single frozen instants now (mid-bite, mid-sweep, mid-swallow), `PROMPT_FORMAT_CORE` leads with **ONE FRAME, ONE ACTION** (length is spent on DETAIL, never on more events; if a prompt could describe two shots, add specificity, never another moment), rule 7 went from "CONSTANT MOTION" to **ONE MOTION, CAUGHT MID-WAY** — the old wording asked for movement and never named a ceiling, which is what let one prompt carry three — the self-check gained "count the moments", and Enhance was told that detail means the prop, the grip and the light, never another beat (it was the one path explicitly asked to come back *richer*, so it grew prompts by adding events). Continuous never had this bug: its `KEYFRAME_FORMAT` has said "THIS IS A START FRAME … its OPENING INSTANT" since it shipped, and Line-by-Line was the gap. **The mechanical half is `withSingleFrame` / `withSingleTake`**, appended in `startImageTask` / `startVideoTask` exactly like `withNoOnScreenText` and for the same reason: a writing rule binds the chat model and nothing else, so a hand-edited prompt, an imported storyboard, a card written before the rule, or a session resumed from history was uncovered. Both are unconditional rather than gated on `noRealism` — a keyframe is one frame in any look. Video takes the TAKE clause, not the frame one: a clip has time and may play its one action out, what it may not do is cut, which is the same thing B-Roll has always said by never sending `multiShots`.

**A still and the clip it animates into are TWO prompts, and the second one is one or two sentences** (August 2026, `MOTION_FORMAT` + `PromptVariation.motionPrompt` in `services/generateBroll.ts`, `CardState.animateMotion`). The Animate tab fired the card's own image prompt, which is by construction a frozen instant — one action caught before it lands, per the ONE FRAME note above. Handed back to a video model that is already *holding* that frame as its start image, the whole paragraph reads as "draw this picture", so the model spends the clip re-rendering what it can already see and the character barely moves: reported as "when I animate it the character doesn't do much". The storyboard now writes a **second, much shorter prompt per variation** — `<MOTION>`, one or two sentences of the same single action playing OUT, ending on a clause saying the camera stays locked — and the Animate tab is that field, not the shot paragraph. Everything else is unchanged: the Image tab and the Video tab still fire the still prompt, because a from-scratch clip has no frame and genuinely needs the picture described. **THE CAMERA NEVER MOVES, and the motion never says so** (both Massimo's calls, in that order): this is UGC filmed on a propped phone, a video model given no camera direction invents a push-in on every shot, and a camera move is also the fastest way to lose the character's face — the model has to invent geometry the start frame never showed it. So `MOTION_FORMAT` bans every push, pull, zoom, pan, tilt, orbit, track, drift, sway and rack focus **by name** — and bans "the camera holds steady" with them, because the box is for what MOVES and a sentence about a camera deliberately doing nothing is a sentence the member reads past every time. The lock is guaranteed downstream instead, by **`withLockedCamera`** in `services/realism.ts`, appended at fire time beside `applyStyleToPrompt` and the voice profile so the persisted prompt stays what the member typed. It sits alongside the three clauses already there and for the same reason — a writing rule binds the chat model and nothing else, so a hand-typed motion, an import or a resumed session was uncovered — and it is armed by the presence of the motion argument, i.e. by this being an animate run: the **Video tab is deliberately excluded**, since a from-scratch clip has no frame to hold still. Three things follow. Continuous has worked this way since it shipped (`ContinuousConcept.motionPrompt` → the frame card's `animateMotion`), so this is that idea brought across, not a new one — the camera rule is the one place the two deliberately differ. **An empty motion falls back to the still prompt** at fire time (`animatePrompt` in `VariationCard`, mirrored on the modal's disabled gate), which is what a session generated before this shipped, an import written against the old envelope, and a hand-added blank option all are — taking Animate away from those would be worse than the behaviour they already had. The modal used to spell that fallback out in a line under the box; it came off (Massimo's call) — it explained a case the member never has to think about, under a field that needs no instructions. And a **dialogue** motion carries the spoken line verbatim in quotes exactly as the shot prompt does, because it is the whole prompt an animated talking clip fires with; that makes it the fourth place `handleEditSceneLine` has to swap the words, alongside the scene, the variation's prompt and the card's `editablePrompt`.

**A card's three hover shortcuts are ONE pill, split by DASHED hairlines** (`DETAIL_SHORTCUTS` in `VariationCard.tsx`, September 2026, Massimo's call). Image / Video / Animate open the detail modal on that tab; they were three separate pills, each with its own rim and its own 6px of gap, which on a 110px card is more chrome than label. One rim around the lot spends the gaps on the words instead. The divider is dashed rather than solid on purpose: a solid rule reads as a seam between two controls, a dash reads as a perforation in one. Each segment fills with the app accent on ITS OWN hover, so the side you are over is the side that will open — that is the whole reason the three labels are there rather than one "Open" button.

(History is a **RAIL on the right of the storyboard**, not a tab sharing its pane (September 2026, Massimo's call — the same pass that moved Scripts' and Voiceovers'): 280px from 980px up, standing in FRONT of the storyboard below that, where picking a session hands the pane back. Its band leads with **New Storyboard** (`RailNewButton`) and gives it the whole width. It is the canvas reset the header's **+** used to be, and since September 2026 (Massimo's call) it **also resets the setup column** — product, character, script, the pasted script text, the extra instructions and the Ad Analyzer blueprint behind `sceneStaging` — because a "new storyboard" that leaves the last session's cast in the inputs is not a new storyboard. The blueprint goes with them even though no row displays it: it is the only source of scene staging, so one left behind keeps staging shots for an ad the member has moved on from with nothing on screen saying so. Nothing is deleted (the session is a row in the list underneath, media and all), but the inputs are the half no row hands back until you open it, so this one **arms first** — one click turns it monochrome and says *Confirm*. Open, the rail is shut from the **lip on the seam** (`HistoryRailHandle`); shut, the way back in is the labelled *History* toggle, which moves INTO the storyboard strip (passed down as `railToggle`, right end, outside the scroll port), where the bar's own glass runs under it and the storyboard keeps its full width; a column in the rail's place narrowed it by ~135px. Both controls live in the root file, which carries the reasoning and the breakpoint rule. The column survives only where the bar can't take it, which is now just the empty state — Continuous stopped wrapping below `md` when it took the Line-by-Line strip's one-line shape, so the shut toggle rides the bar at every width in both tabs. Below 980px the rail covers the storyboard entirely, so the lip goes with it and the rail's band carries `RailCloseButton`. **This pane has no header band at all** now — it held the Storyboard / History `SegmentedToggle` and nothing else. **A card's detail modal arrives on `.modal-pop` / `.modal-fade`** (September 2026, Massimo's call), the Characters preset picker's own 200ms move, and so do both Continuous modals — they share `ModalShell`, so that is one edit for the pair. The card grid's columns are a **container query** (`@min-[520px]` / `@min-[820px]` / `@min-[1100px]`), not viewport breakpoints: `sm:grid-cols-2` put two 130px cards side by side inside the 280px rail. `HistoryRail` — a **card grid**, day-sectioned under a centred `DayPill`, not a list of thin rows: a session's own footage is the thing worth recognising, and 40px round thumbs on a 70%-wide panel showed almost none of it. Each card is a media cover over a two-line title + meta, and that block is **CENTRED** (Massimo's call, September 2026) — under a full-bleed cover it reads as the card's caption rather than as the start of a left-hand column. The centring is this card's alone: Scripts' rail rows stay left-aligned, having been tried centred the same day and put back, because a row is a line of chrome down a narrow column rather than a caption under a picture. **mode filter pills** — All / Line-by-Line / Continuous, keyed off each row's derived mode (`brollHistoryMode`), shown only when >1 mode is present; delivery is deliberately not a pill, since a dialogue run is the same kind of session as a silent one — plus a persisted **sort dropdown** (`broll-studio:historySort`: Newest / Oldest by `createdAt`, Recent by `updatedAt`) that drives ordering + day-grouping + the row's relative-time label together — the shared `Dropdown`, so its menu PORTALS out of this rail's `overflow-y-auto` column; a locally-positioned one was clipped down its left edge the moment the list became 280px wide. **The sort LEADS the search row and the mode pills are a centred row under it, rendered only when they have something to say** (September 2026, Massimo's call). It went through both other shapes first: one wrapping row (right in a wide panel, wrong in a 280px rail — the field sat at its `min-w-[200px]` floor with dead space beside it and the sort wrapped underneath, left-aligned against the field's edge), then the sort centred on a row of its own, where it read as orphaned — a lone pill floating in the middle of a column with nothing to be centred against, costing ~40px of a rail that is otherwise all list. It is a filter, so it belongs on the line with the other one: `shrink-0` at the left, the field taking the leftover, which at 280px measures 95px and 152px with the placeholder still fitting. That fit is also why the sort labels are **one word each** — "Recently updated" alone is ~105px of text, and a trigger whose one job is naming what's picked must not truncate. The second row now renders only when a mode pill or a live queue chip is in it, so a rail holding one delivery spends nothing on an empty band. Cards carry a **mode badge** (top-left) + scene count and a **visual-style pill** (`historyStyleLabel` → `getContinuousStyle` label, else the saved style's `styleName`, else "Custom style"; the row-level `styleId`/`styleBrief`/`styleName` snapshot covers Continuous, whose result carries no brief) over the cover, and a finished-media tally (`mediaTally` → "6 stills · 2 clips") beside the timestamp below it. The **cover** is a mosaic of up to three pieces of the session's own media (`historyCovers` — 1 full-bleed / 2 split / 3 as big-plus-stacked-pair), drawn from either mode: stills first, then clip first-frame posters; a session with no media yet gets the film glyph. Delete sits in a `TileActionStack` on the cover (media variant), and an in-flight session gets the rect `GeneratingPulseRing` + its live count in place of the tally. Rows from the retired One-Shot mode are **filtered out of the list** by `isRetiredOneShotRow` (applied in `RightPanel`) — the rows and their clips stay on disk and in the cloud, so the mode can come back without losing anyone's work. `BrollHistoryItem.createdAt` is **write-once** (`upsertBrollHistory` preserves it) and an upsert whose content is unchanged is a **no-op**, so merely reopening a row neither bumps it under "Recently updated" nor re-pushes to the cloud; the FIFO cap evicts by `createdAt`, not by array position. Search matches the row's displayed title (product · influencer · script, resolved once in the parent) as well as `inputSummary`.) **The input column is a `SectionCard`** (August 2026): Product → Character → Script all sit inside one **References** card, with **Import** on its centred header's RIGHT edge (one word — "Import prompts" wrapped to two lines in a narrow column; the popup keeps the full name) — the card header replaced the left-aligned label row that used to carry it, so the group costs no extra row. It shared that header with a **New** pill for a while, which is what pushed Import to the left gutter; the pill went in September 2026 (Massimo's call — see the root file's `ClearAllButton` note, which took `handleClearInputs` with it) and Import took its edge back. Clearing the canvas is still the right panel's **+**. The **script is LAST** in it because it's the only reference here that grows as you paste; above the two pills it shoved them down the column on every keystroke. Additional Instructions stays OUTSIDE the card — it's where you type, not what you attach. **All three rows in that card carry a `required` `StatusDot`** at their left edge (August 2026), so an empty one is red — Product and Character used to be neutral. Strictly only the Script IS `canGenerate`; the product and the character are what a UGC storyboard is OF, and a run without them fires but isn't worth the credits. **Empty, every row wears the same dashed surface**; **filled, each lights up in its own bank's colour** — amber products, pink influencers. Both states were flattened to neutral for a day and put back: the accent IS the "this is set" signal, and which accent says which bank set it, so a neutral fill made a chosen reference the quietest thing in the card. Strictly only the Script IS `canGenerate`; the product and the character are what a UGC storyboard is OF, and a run without them fires but isn't worth the credits — which is the reading the other dot colours would have had to carry anyway. That fixed a real lie: the Script slot used to wear an **Optional** tag and offer "Or let the format write it" — a feature deleted in July 2026 — while being half the gate, so an empty script left Generate grey with nothing on screen explaining it. It now reads "Pick one from your bank, or paste it below" (the `optional` prop is gone from `BankCard`, replaced by `required` + a `filled` override, since a paste fills the input while the header row is still the "empty" bank state). **Two modes** via a left-panel header toggle (`:mode`, default `'line'`; order Line-by-Line → Continuous, aligned with the right panel's Storyboard/History strip), and **whether the cards speak is NOT a mode**: `BrollDelivery` ('dialogue' / 'silent') is a **Dialogue Clips / B-Roll Clips** `SegmentedToggle` sitting directly UNDER the References card and above the Instructions box (`:lineDelivery`, hidden in Continuous) — it decides what KIND of storyboard the references just gathered are for, so it reads as the answer to the card above it rather than as a setting down in the generate band. `h-12` and `shrink-0`, with `mb-2` of its own: it's a fixed row between two boxes that split the column's leftover height, and the card's `mb-2` only spaces the gap above it. Its two segments read **B-Roll** and **Dialogue** — they said *B-Roll Clips* / *Dialogue Clips* until September 2026 (Massimo's call), where the shared word carried no difference between the two and only ate the width the names needed in a narrow column. **B-Roll Clips leads the toggle AND is the default** (September 2026, Massimo's call). Position and default were deliberately split for a month — the plainer thing first, the default on the one most members were here to make — and that reading is what changed, not the reasoning: an opening storyboard of silent b-roll is what most sessions want, and a member laying dialogue over it is picking a mode rather than correcting one. **The pick is NOT persisted**, and that is the other half of the same call: it was a `:lineDelivery` slot, so one flip of the toggle — or opening a Dialogue session from History, which sets the toggle to match the row — made Dialogue the way the app opened from then on, forever, and the default stopped being a default the first time anyone touched it. It is plain `useState` now: every fresh load of the workspace opens on B-Roll Clips, and a Dialogue pick lasts as long as the session it was made for. The app stays mounted behind a dock switch, so leaving B-Roll and coming back keeps the pick — it resets on a reload, not on a tab change. (The toggle has also led the settings band, sat at that band's bottom directly above Generate, and sat at the very top of the column above the References heading.) Dialogue was promoted to a third mode for a day and folded back: a mode is a different shape of workspace, and both deliveries produce the same per-line storyboard. `isLineMode` / `sanitizeBrollMode` in `types.ts` own the coercion — a persisted `'broll'` / `'dialogue'` / `'oneshot'` / `'animated'` mode resolves to a live one. The delivery used to be recovered out of that raw `:mode` slot (`readPersistedMode`, deleted with the persisted slot) so a member mid-session in the short-lived Dialogue mode reopened on Dialogue Clips; nothing reopens on Dialogue any more, by design. History rows are unchanged: `mode: 'line'` + `lineDelivery`, stamped from `sessionDelivery` (what the run actually used) so a toggle flip afterwards can't rewrite a finished session. **The Ad Format picker is removed** (July 2026), a week after B-Roll stopped writing scripts. It offered Scripts' Formats + Structures list plus a `'standard'` Standard UGC card, and it staged the shots; once the words came from elsewhere, staging was all it did, and the storyboard reads better staged by the script and the Visual Style than by a label the member had to set before Generate would light up. The row, its slide-over and the `adFormat` state are all gone — the state especially, because leaving it without its picker would mean a persisted format quietly staging every shot with nothing on screen to see or change it. `AdFormat`, `sceneStagingFor` and `BrollInput.sceneStaging` all survive untouched, and `AdBlueprintPayload` is now the **only** thing that feeds staging, so restoring the picker is re-adding a row rather than rebuilding the seam. The **settings band** is now `ScriptModelRow` (`broll-studio:chat`) → Generate, with the delivery toggle a free-standing row between the References card and the Instructions box (August 2026): the card is what the storyboard is built FROM, and the delivery is a decision about what to build, so it was the one switch in a list of things you attach — and the hairline it needed inside the card to say so was a rule admitting the row was in the wrong box. Both came out together. **Visual Style is not picked in this column at all** (August 2026, see below): it went from band row, to third row of the References card, to the storyboard's own caption pill. The model row is 58px with a 36px icon disc and a name over a one-line subtext, exactly like the cards around it; the delivery toggle is `h-12`, matching the Characters generate bar's 1K / 9:16 pills. The band sits **8px under the Brief box**, the same gap the reference cards sit apart (the column's `pb-2` plus the band's `pt-0`); it was 26px, which read as a divider. The band has **no card of its own**: no border, no tint, no uppercase eyebrows, no Required tags (a greyed-out Generate says that on its own) — a bordered block inside the panel drew a panel inside the panel. It keeps an opaque fill only in its mobile `sticky` form, where it floats over scrolling content. It drives every chat call B-Roll makes, in both modes: the storyboard, per-card Enhance, per-card Regenerate, and Continuous' motion rewrites. **B-Roll no longer writes scripts** (July 2026): `services/autoScript.ts`, Scripts' `writeOneScript`, the length toggle under Ad Format and the two-chained-call Generate are all deleted (see git history). Writing a script is Scripts' job — it has the modes, the angles and the takes to do it properly, and a one-take copy living here was a second thing to keep in step with no way to compare what came back. Consequences: the script box is **required** (`canGenerate = hasScript && styleChosen`; the format is deliberately NOT in the gate, since pre-format sessions have a script and no format, and a storyboard without staging is a plain UGC storyboard rather than a broken one), and the state formerly called `autoScriptStyle` is now `adFormat` — **its persisted key is still `${baseKey}:autoScriptStyle`**, because renaming it would silently drop every member's pick. **The References card takes 5 shares of the column's leftover height to the Instructions box's 1** (`flex-[5]` vs `flex-1`, August 2026): they split it evenly at first, which gave a one-line steer the same room as the words the whole storyboard is written from — and the card spends most of its share on the three pills above the script, so an even split left the script box the smaller of the two. Neither is a fixed height, because a box that stops growing starts scrolling inside itself (the trap `AutoGrowTextarea` exists to avoid). The card's content column must NOT be `min-h-0`: that zeroes the whole card's min-content, so on a short window the column squashed the card — script box and Instructions clipped clean away — instead of scrolling. The floor lives on the growing child and has to be able to propagate up. **Script / Hooks and the Instructions box previously shared that height evenly (`flex-1`, no `basis-0`)** — each one's base size is its own content, so a pasted script grows its box while an empty brief stays a strip, and whatever is still spare is split between them. With `basis-0` they were an even split whatever was in them: a matched pair on an empty panel, and a script fighting for room the moment one was pasted in. Section labels in this column (References) and the storyboard's scene count use the Voiceovers side panel's subheading style: 13px, sentence case, `text-ink-200` — not a small-caps eyebrow. The picker also carries **`'standard'` — "Standard UGC"**, B-Roll's own option pinned above both sections (`AdFormat = WriteStyle | 'standard'`): no format imitated and no mechanic imposed, so no scene staging is appended and the shots come out as plain organic UGC, for an ad that shouldn't be disguised as a podcast clip or a street interview. It is **not** a default — nothing is picked until the member picks it. Its picker is the full Script Style list (10 Structures + 7 Formats, single pick, same slugs, **Formats first**). The "Additional Instructions" box doubles as the brief and stays optional, because the product row already carries pain points, USPs, benefits, objections, offer and CTA. **The picked format also stages the SHOTS**, in both modes and whether or not the script was written here: `sceneStagingFor` hands both storyboard calls the same `WRITE_STYLE_SCENE_DIRECTION` block Scripts stages a blueprint with (`BrollInput.sceneStaging` / `ContinuousInput.sceneStaging`), so a podcast-clip session gets boom mics and studio angles instead of the default selfie-to-camera. Structures carry no staging on purpose — an argument implies no camera. Both prompt sites append a token guard, because that block writes `[CHARACTER]` / `[PRODUCT]` for a format that has reference slots and a B-Roll prompt is plain prose an image model reads literally. **The Generate button reads "Generate Storyboard" in all three cases** (both deliveries and Continuous), and "Storyboarding..." while it runs. It used to name the delivery back at you — "Generate Dialogue Prompts" / "Generate B-Roll Prompts" / "Generate Storyboard" — which was three names for the one thing the button makes, chosen by a toggle sitting directly above it that already says which kind you're making (and an icon that says it again). Both Generate buttons carry an estimated-credits pill (`services/promptCost.ts` — chat is per-1k-tokens, so it's a deliberately rough, rounded-up figure that lands under a credit or two; it exists so nothing fires unpriced). **Import prompts** (`services/importPrompts.ts` + `ImportPromptsModal`, a grey pill paired with the panel's **New** reset in the References eyebrow row — same size and style as `ClearAllButton`, present in both modes): paste — or drop a .txt/.md/.xml/.json file of — a storyboard written outside the app and skip the prompt-writing call entirely (the member's own Claude subscription instead of their kie credits). Two rules hold it together: it runs through **the same parsers** the live call uses (`parseScenes` / `parseContinuousResult`), and the modal's **Copy brief** hands the outside model **the app's own system + user prompt** for that mode (`brollSystemInstruction` + `buildBrollUserPrompt` / `CONTINUOUS_SYSTEM` + `buildContinuousUserPrompt`) plus an output-contract tail — so the brief can't drift from the parser when either is edited. Parse feedback is live (counts, non-blocking notes, and a wrong-mode hint off `detectImportMode`); committing starts a fresh session exactly like a Generate (new sessionId, cleared card states, style stamped) and backfills an empty script box from the storyboard's own lines. Deliberately ungated by script/style — the prompts already exist, and style is only appended at render time. **One-Shot mode is retired** (July 2026): whole script → one multi-cut clip. No model on kie could hold a 30s ad together well enough to ship, so it burned video credits on footage nobody used — revisit when a model can (Seedance 2.5 was the trigger Massimo named). Its view, detail modal and service are deleted (see git history to restore); what stays is the persisted data — `BrollHistoryItem.oneShot*` + `mode: 'oneshot'` in `stores/types.ts`, kept so no row or clip is lost, hidden from History by `isRetiredOneShotRow`. Shared pieces it left behind, still used by the two live modes: all three detail modals wear the **`SectionCard`** shape their input column does (August 2026) and read **style note → group card → prompt → footer**: References around the ref slots + `ProductPhotoRow` + `ExtraRefsRow` in the per-line and frame modals, **Keyframes** around the clip modal's two endpoint thumbs *and* the amber "pick both ends" line (they're one group and they're the gate), and the dialogue card's **Voice** box promoted from a hand-rolled tinted look-alike to a real card. The clip modal's style note moved to the TOP for that reading order — it opened on its keyframes, so each of the three modals started with a different thing. Prompt boxes stay uncarded (where you write, not what you attach) and the footer bands stay borderless, exactly as in the input column. The only `StatusDot`s are the ones that gate a run: Animate's **Start frame** and the clip's two **keyframes** (red when empty); the Animate tab's **Motion** box is neutral-when-empty because it doesn't gate that button, and the reference slot cards carry none at all. Every chip in a card footer is `size='lg'` (h-12), the height of the take stepper at the end of that row and of the model trigger above it — they were the default `md`, so a row that reads as one control strip was three heights — and the modal's own rhythm is 8px throughout (August 2026). All three also share one compact header (`h-12`, `pt-3`) — serif `01` number, vertical rule, one identity pill stacked over the script line; the per-line card modal's tab strip runs **Image → Video → Animate** and lands on Image (the still is the first thing the card needs, and the clip is rendered or animated off it), matching the card face's hover shortcut row and a plain click on the card; the gallery's full-width **Animate B-Roll** hover bar on an image tile is **Image-tab-only** (`ModalGallery`'s `onAnimateImage` is omitted elsewhere) — its whole job is carrying a still over to the Animate tab, so on Animate or Video it pointed at the tab you were already on; `ModalVideoPlayer` (`cardDetailParts`) gives every modal video tile play-with-sound; `ModelSidePanel` takes `allowedModelIds` (which models are LISTED) + `enabledModelIds` (which are selectable — the rest show greyed/struck-through, same look as `requireMode` dimming) and renders **capability filter pills** (Start + end frame / Start frame / Reference images / Text to video, multi-select) for the video task. **Line-by-Line** (either delivery): script → scenes → **3 variations/scene** (`VARIATIONS_PER_SCENE`), rendered in one row alongside a normal "Add option" card (four equal cells at `@[840px]:grid-cols-4`; a scene carrying 4 variations — an added option, or a session generated back when Dialogue Clips emitted a fourth card — reaches `@[1040px]:grid-cols-5` so its Add card doesn't wrap alone). **Those steps are CONTAINER queries, never viewport ones** (September 2026, Massimo's call): what squeezes this grid is the storyboard column, which loses ~340px the moment the History rail opens, so an `xl:` rule kept four cells on a 2000px screen and shrank every card to ~170px to keep an empty Add card on their line. Below each threshold the row narrows and the **Add card wraps onto a line of its own** — it is the cell we can afford to lose, and the tiles the member is judging keep their size. It was 4 and the fourth was reliably the dud — three good ideas beat three plus a filler, and every card the member doesn't use is an image (often a video) on their own credits. The parser still READS a `<VAR_4>` (`MAX_PARSED_VARIATIONS`) so older sessions and imports of pre-cut storyboards keep every prompt they were written with. Both storyboard parsers read their blocks through `services/xmlBlocks.ts`, which is tolerant of a missing closing tag — a strict `<TAG>…</TAG>` match dropped a whole `<VAR_3>`/`<CONCEPT_3>` when the model forgot to close it, which is how scenes came back with two cards instead of three. Leaf fields end at the next tag; containers end at their next same-family sibling (never at a closing tag, which would cut a scene off at its own first `</LINE>`). **Dialogue Clips** (`DIALOGUE_DELIVERY_ADDENDUM`, read last so it beats the silent doctrine everywhere): **all three** cards are `DIALOGUE` cards speaking the scene's line verbatim, and the column they sit in decides what they are. **VAR_1 of every scene is the ANCHOR TAKE** (August 2026): one place, one spot, one wardrobe, one light, one camera position, held across the WHOLE ad, so reading down the VAR_1 column plays as a single sitting cut into pieces — the one-place talking-head ad. Only the CUT changes between scenes (angle, distance, hands, posture, the expression the line lands with); the prompt makes each VAR_1 **restate that setup in full, in the same words**, because each scene renders as its own separate image and a setup that is only implied by an earlier scene drifts into a different room. **VAR_2 and VAR_3 are the two alternatives** and they still move — a different room, a different thing in their hands, a different moment — so a line that doesn't work in the anchor spot has somewhere to cut away to. This pairs with the header batch's **Options chip row**: scoping a run to Option 1 renders exactly the anchor ad. A scene-staging block doesn't override the split, it scopes it — the anchor is one spot *inside* that format (one stretch of pavement, one seat at the desk) and the alternatives move within the same format. The picked Script Style's staging governs what "different" means (three spots along the same street for a street interview, three moments of the same routine for a GRWM); with no format picked it's a plain organic UGC video and the character moves around their own life. This replaced a shape where VAR_1 was the only talking card and VAR_2/VAR_3 were silent b-roll, all locked to ONE continuous take — same room, wardrobe and lens height for the whole ad, each card chained to the previous one's still. Both halves were wrong for the delivery: silent footage is what B-Roll Clips is for, and three angles on one sitting is one idea filmed three times, which is exactly what the b-roll prompt spent its whole length learning not to do. Consequences: `clampRefsToVisibility` no longer forces `'character'` on a DIALOGUE tag (every card is a talking card now, so that exception would keep the product out of the entire ad — a line that names the product still has to be built from the real packaging), and `CardState.chainLink` **defaults ON again** (August 2026, with the anchor take) — but it only ever bites on the anchor card, because `dialogueChainRefs` hands a `chainImageRef` to the first DIALOGUE variation of each scene and nothing else. On the other two cards the flag is inert: no reference attached and no "Previous cut" slot. **A batch arms every card in one tick, anchor cards included** (August 2026): they used to run as a queue instead, one armed each time the previous one's still landed, so card N could chain from card N-1's picture. That cost the whole run — `dialogueChainRefs` only feeds the FIRST variation of each scene, which IS the anchor column, so scoping a batch to Option 1 (the common case, one card per line) rendered one card, waited a minute, then started the next; twelve lines took twenty minutes and eleven of the twelve cards showed nothing at all, which reads as a run that never fired. The chain is best-effort now: a card attaches the nearest earlier anchor still that EXISTS when it fires, and a fresh run has none. What holds a fresh anchor column together is the prompt, which already restates the same place, wardrobe, light and camera in every scene's VAR_1; the backend stagger is `submitToKie`, which drips the POSTs under kie's rate limit while every tile shows generating from the press. It has to be on, because prose alone cannot hold a room across four separate image generations — `buildDialogueChainPreamble` ("recreate its world exactly, this is the next cut") is what actually keeps the anchor's place, light and camera position from drifting, and it's the same principle as the no-text clause: deterministic at fire time, not hopeful in the prompt. Sessions written while it defaulted off stored an explicit `false` and keep their behaviour. Identity is held by the character reference, which does it better than a chain image can. One shared `<VOICE_PROFILE>` is still appended to every dialogue clip's video prompt at fire time. **The spoken line is editable in place**: the scene masthead sets both its take number and its quoted line in **Instrument Serif italic** (the number one step up the ink ramp at `ink-700` — at `ink-800` it was faint enough on a phone to read as a rendering artifact), and clicking the quoted line on a scene row opens `SceneLineEditModal`, and saving runs `services/scriptLineEdit.ts` — a deterministic swap of the quoted words inside every prompt of that scene (`swapQuotedLine`, tolerant of curly quotes, wrapped whitespace and moved trailing punctuation), plus the panel's script box when the line is one of its lines (`swapScriptLine`, which no-ops rather than guessing, since the storyboard may have split a sentence). No LLM call, no credits, and the room/gesture/light in each prompt survive untouched; the per-card Regenerate prompt is still there when the new line needs a different shot. Three copies of the line move together — the scene, each variation's `prompt`, and each card's `editablePrompt` — and the swap is deliberately NOT pushed onto `promptHistory`, so Undo can't desync a prompt from the header it's speaking. Under **B-Roll Clips** all three variations are **3 genuinely different concepts** for visualizing the line (show-don't-tell: metaphors made literal — "tasted like cardboard" → the character biting actual cardboard — at least one LITERAL variation when the line has a vivid image), LLM-tagged with a lens from a 7-tag menu (Action / Emotional / Product / POV / Environment / Transition / Proof); parser falls back to positional buckets when `<TAG>` is absent. (`ALL_TAGS` is the silent-b-roll menu offered to the model; `PARSEABLE_TAGS` adds `DIALOGUE` — emitted for VAR_1 in Dialogue Clips — and legacy `STATIC`, whose `buildStaticReferencePreamble` + `startImageTask` `inheritReference` opt survive so old persisted sessions keep rendering.) **Prompt shape:** every prompt is ONE flowing paragraph with **no word limit** — the 40-80-word guide was cut because it capped the model's vision mid-idea; vagueness is the only failure, length isn't — no labelled fields (`PROMPT_FORMAT`, shared by all three prompt sites so they can't drift) — the old six-line `SETTING / CAMERA / ...` structure is gone (it read disjointed and crowded out the idea). Prompts stay silent — no speech, no music — and `withIphoneRealism` appends the realism stack as a trailing sentence (its CAMERA-line branch survives only for legacy structured prompts). `generateNewVariation` / `enhanceVariationPrompt` keep the `<PROMPT>` tag envelope, not JSON. **The camera is a viewpoint, not a prop:** all three prompt sites forbid naming the filming device (phone / front camera / tripod / mirror selfie) and require camera position as geometry — height, distance, angle — because any device noun gets drawn into frame; naming the iPhone as a *quality register* stays fine. PROOF is the one sanctioned exception (a screen as the subject). Every image/video gen gets a deterministic iPhone-realism suffix appended at request time (`services/realism.ts`). **The no-on-screen-text guarantee lives in the same place and for the same reason** (`withNoOnScreenText`, August 2026): "no captions, subtitles, or on-screen text" was a rule in every prompt-WRITING instruction and in none of the requests those prompts end up in, so the chat model had to obey it while writing and the image model never heard it at all. Everything downstream was uncovered — the render itself, a hand-edited prompt, an imported storyboard — which is why it surfaced as a model regression when the default chat model changed rather than as the standing gap it was. It bites hardest under Dialogue Clips, where the prompt by design carries the spoken line verbatim in double quotes and an image model handed a quoted string *draws* it: talking-head stills came back with their own line burned across the frame as a TikTok caption. So the clause is appended to both `startImageTask` and `startVideoTask`, and **unconditionally** — unlike the realism stack it is NOT gated on `noRealism`, since a claymation ad has no more business carrying burned-in subtitles than a UGC one. A prompt that actually quotes something also gets a second half naming those words as audio ("spoken aloud … never as text on screen"), which is what stops the model reading `says: "…"` as a render instruction; a silent b-roll prompt has no quotes and doesn't get it. Both are idempotent, so a retry of a persisted post-suffix prompt doesn't stack them. Captions are Edit's job, over finished footage — never rendered into the picture. `appliedStyleNote` carries the clause in both its branches, because that note is the only place a member can read what rides outside their prompt. **The storyboard picks which product photo each shot gets** (`services/productAngles.ts`): a product's photos are `[productImage, ...extraImages]` (`productPhotosOf`), and when there's more than one they're attached to the storyboard call as numbered vision inputs so every variation/concept can declare `<PHOTOS>` — the state its shot is actually in. Someone biting the bar gets the unwrapped photo; someone pulling it out of a drawer gets the sealed packshot. The first pick IS the product reference (`productRefsForSelection`), anything past it rides behind as an angle, and the default when nothing is declared (legacy rows, imports) is the hero photo alone. Attaching every angle to every card was the old behaviour and it put TWO products in frame — a character eating the bar beside an identical sealed one — because an image model draws what it's shown. Per-card override is the modal's `ProductPhotoRow` strip (hidden for single-photo products); nothing the user picked is ever displaced, since the angles are the lowest-priority refs and clamp to zero before a manual one is dropped (`referenceImageCapacity`). The preamble builders still carry a one-object-several-shots clause for the multi-photo case (`productAnglesClause` / `buildContinuousPreamble`'s `productAngles`), now stating outright that exactly ONE product appears in frame, and the Product slot card reports what will really be sent ("Product · +1 angle"). **A card modal's gallery is a two-column GRID, never CSS multi-column** (August 2026): it was `columns-2 [column-fill:_balance]` with `break-inside-avoid` per tile, and multi-column balances from the heights it knows at layout time — but an `ImageTile` is `h-auto` until its blob decodes, so every picture that arrived changed the numbers behind the balance and the browser re-fragmented. `break-inside-avoid` is a request, not a guarantee, and the two artifacts it let through were reported as bugs: a tile's border box split across the break, stranding its top edge in the other column as a stray purple line, and an empty leftover box at the foot of a column reading as a black card with nothing in it. A grid cell can't fragment and there's no balancing pass; `items-start` keeps each tile at its own height. **A card's Retry RESUMES rather than re-fires whenever the entry still holds a kie `taskId`** (`resumeInFlightVideo` / `resumeInFlightImage`): the generation is already paid for, and the failure this exists for is the DOWNLOAD stalling on a clip kie has already rendered — the biggest files in the app (Seedance 2.5 at 720p with audio) were timing out on the CDN hop, and the only button on offer re-ran the whole thing at full price. kie's result URLs live 3 days, so a resume is free and usually instant. Only a gen that never reached kie — no taskId — is genuinely re-fired. A poll timeout stays the separate case it already was: the entry is left in flight rather than flipped to Failed, because that one really is still rendering. Each card has Generate Image **and** Generate Video; video gens are fire-and-forget with refresh-safe resume. Both are also **batched**: the storyboard header carries one **Generate all** whose menu holds the three passes — **Generate all images**, **Animate all stills** (`stillsOnly` on the video `BatchRequest`, listed only once some card holds a still and scoped to the cards that HAVE one), **Generate all videos** — and each scene row carries its own compact **Images** / **Videos** pair, all routed through the same per-card token idiom. The three were three pills on the bar until August 2026 (Massimo's call), collapsed into a menu because three long labels are ~450px of a panel that is 498–677px wide, and because the passes are mutually exclusive in practice: you run one, wait for it, then run the next. So the step after Generate-all-images animates exactly what's on screen and nothing renders from a prompt the member hasn't seen a frame of. Both storyboard strips run **one row from `md` up** (`md:flex-nowrap`, meta shrinking and truncating, buttons `whitespace-nowrap`): with `flex-wrap` alone the four batch pills plus the meta outgrew the panel at 1280 and at any real zoom level, and the answer was dropping the whole button group onto a second line — so the strip was two rows tall on some windows and one on others. Both strips (here and Continuous) are **static rows ABOVE their scroll port, not `sticky` inside it** (August 2026) — `relative z-20 shrink-0` over an opaque `bg-surface-0`, with the scroller taking the `pt-5` that used to be the strip's `mb-5`. They were sticky and glass, and they visibly came loose from the top edge on the way back up a long storyboard: a backdrop-filter element re-samples its backdrop on the main thread while the scroller is scrolled by the compositor, so the bar lagged its own container and snapped back. Outside the scroller it cannot lag by construction, and neither strip ever scrolled away anyway (both were pinned at `top-0` from the first pixel), so all that's given up is cards passing under them blurred — which is also why the glass came off: over a panel background that never moves it's a backdrop root's cost for no picture. The panel header above them is still glass and still `z-30` (real content does scroll under that one). Earlier history worth keeping: they were opaque once on the theory that a nested backdrop-filter can't re-blur inside the window frame's own blur — it can, and what made a translucent strip read as unpinned back then was too little blur and no saturation; and their `sticky` had already needed `App.tsx` to stop flooring the app pane's height, since a second scroller above the panel carried both bars up the screen (see the root file) (`generateImageToken` / `generateVideoToken` + a `batchImageOverride` / `batchVideoOverride` the run picks once, leaving each card's own persisted settings alone). **A header batch is scoped by option COLUMN, and both dialogs open on ALL options** (August 2026): the storyboard is a grid (a row per line, a column per option), and both confirm dialogs carry an Options chip row — Option 1 / 2 / 3 / All options — that re-scopes the run live (count, cost and the skip copy all follow), with a finished column ticked. They used to open on `firstOpenColumn` — the leftmost column with anything still ungenerated — so a second press picked up where the first left off without a stateful counter to get out of step, and a member who stopped after one press paid a third of the credits. That default is gone for both: a button labelled "Generate all images" / "Generate all videos" that quietly runs a third of them is a button that doesn't do what it says, and members read the short run as a bug rather than a saving. Scoping to one column is still one chip away; it is the deliberate act now, not the default. Cards that already hold a still/clip are still held back behind their own tick, so "all options" means every option not yet generated, never a re-bill of finished work. The per-scene row buttons pass `columnar: false` (one card per column there, so the choice means nothing). A video batch animates the card's COVER STILL when it has one (that's the trip after Generate-all-images) and renders from the prompt when it doesn't; cards that already hold a clip are held back behind an explicit tick, since a clip is the expensive half of the app. Its confirm dialog picks model + resolution + duration for the whole run and defaults to **480p / Auto** on the shared `broll-studio:video` model (Grok 1.5) — the cheap tier, because a batch is a dozen clips at once. **Duration is the exception to "one setting for the run", and it defaults to Auto** (August 2026): a storyboard is a dozen lines of different lengths, so one number for all of them is the flat-5s problem wearing a dialog — `BatchVideoSettings.durationSeconds` is now OPTIONAL, and absent means every card fires at its own length. The Auto chip reads the run's real spread back ("Auto · 5–10s") and the button prices each card at its own seconds, since that's what's billed; a fixed length is still one click away, and a pin the picked model doesn't offer drops back to Auto rather than silently re-tiering the run to a number nobody chose. The dialog's `ModelPicker` greys models that can take neither a start frame nor reference images (`requireAnyModes`), and a model that can do neither also blocks Generate rather than failing on every card. Video resolution follows the picked model's `videoConstraints.default` — fresh cards seed from it and the card modal snaps to it on a model flip (Gemini Omni → 1080p, which costs the same as 720p). **Continuous** (`services/generateContinuous.ts` + `ContinuousView.tsx` + `ContinuousDetailModals.tsx`): Zack-D-Films-style **keyframe-chain** ads — one storyboard LLM call turns the script's N lines into N scenes + N+1 keyframes where keyframe N+1 is simultaneously scene N's end state and scene N+1's start, so every clip is a **frames-to-video** gen (first frame = chosen keyframe N, last = keyframe N+1) and the cuts are invisible. Each keyframe fans out as **3 visual concepts** (`CONCEPTS_PER_FRAME`; same story state, different staging — any pick still chains) — three genuinely DIFFERENT ideas for showing the line, each declaring its own **shot size** off `SHOT_LADDER` (`<SHOT>` → `ContinuousConcept.shot`, chip on the card + frame modal header, fed back via `FrameContext.conceptShot` so a rewrite holds its size). There is no scale mandate: fixed Wide/Detail/Character slots were tried and cut — a mandated Wide on an intimate beat leaves nothing worth shooting wide, so the model invents a set. Plus a per-frame Add-concept (drops a blank card you write or Regenerate a prompt in, like the per-line Add option — no LLM call up front); the user picks one image per frame (auto-picked on a frame's first image; `continuousSelections` keyed by frame index → `{conceptId, imageIndex}`) before any video credits burn. **Chaining a frame to the previous keyframe is ON by default** (`ContinuousFrameCardState.chainLink`, per-frame **Chained/Unchained** pill on the row header beside Generate frame; also a per-concept "Previous frame" slot in the frame modal). It shipped off for a while — all 3 concepts of a frame anchor to one image and come back similar, which weakens the pick-a-concept step — but this is continuous mode, and in practice the continuity is worth more than the spread. Legacy rows that stored an explicit `false` keep it off. The look is held by the storyboard-wide STYLE block plus the character/product refs (pre-style the character in Characters and identity holds far better than a chain image can manage), and the clip is a frames-to-video interpolation that morphs between related frames fine, so long as frame N+1 is a state frame N could plausibly move into. When chained, frame N's gen attaches frame N-1's chosen image FIRST (`buildContinuousPreamble` chain clause, now explicitly forbidding composition/shot-size/angle reuse) plus character/product/extra refs, via `startImageTask`'s `preambleOverride` + `noRealism` opts. **Product visibility is decided per scene** (`<VISIBILITY>` → `ContinuousScene.productVisible`) and refs per concept (`<REFS>` → `ContinuousConcept.refs` → the card's toggles): a beat that attacks the category ("stop eating chalky protein bars") must NOT attach the product reference, or the model renders the member's own packaging as the thing being criticised. Visibility is the hard rule and `parseConcepts` clamps the LLM's refs to it; the prompt must also describe the bad item as an explicit unbranded stand-in ("plain unmarked grey wrapper, no logo"), because omitting the product just lets the model fill the blank from the attached reference. `buildContinuousPreamble`'s `productExcluded` clause says the exclusion out loud so a chained previous keyframe can't smuggle the branding back in. Line-by-Line carries the same doctrine (`clampRefsToVisibility` + rule 2b), and the clamp runs in **both** directions: VISIBILITY=yes forces the product reference ON for every variation of that scene, because a line that talks about the product has to be built from the real packaging rather than the model's invention of it — the LLM kept dropping the ref on lenses that "don't need" it. Still a per-card toggle afterwards. **Continuous does NOT segment the script**: `scriptLines` splits it on newlines, the lines are handed to the LLM numbered, and it must return exactly one scene per line with `<LINE>` verbatim — no splitting, no merging, no reordering. Splitting is the user's call, via `SceneEditModal`. (Line-by-Line still carries the older ONE SCENE = ONE IDEA doctrine, which splits a line at a turn like "most taste like cardboard, **but** this one tastes like cookie dough".) **ONE RULE for a scene:** every keyframe is an ACTION that shows what its line SAYS, **caught at the instant that action BEGINS**, and the concepts are three genuinely different ways to do it. The start-frame half of that rule is load-bearing: a keyframe IS the first frame of its clip, so "mid-motion" got read as "draw the action at its peak" and frames came back with the bite already taken — the clip then has nowhere to travel and the model holds still or races past. The prompts say it three ways (`KEYFRAME_FORMAT`, the ONE RULE block, the self-check) plus in `frameBriefBlock` so a single-card Enhance/Regenerate keeps it; the FINAL frame is the one exception — nothing animates out of it, so it may rest. Clarity is the whole point (a scrolling viewer with no sound should be able to guess the narration), which is why **MEDIUM is the default shot** and `SHOT_LADDER` stops at medium-wide: no aerials, no vast establishing shots, nothing that makes the viewer hunt for the subject. A short-lived `<WORLD>` block (one declared location for the whole ad) and a `<TRANSITION>`/anchor system (a connective device plus an element every concept on both sides had to carry) were both tried and **removed** — the world block made every beat the same corner of the same room, and the transition machinery was ceremony around a prompt whose job is "say what moves". What survives of the chain doctrine is one line: frame N+1 must be a state frame N could plausibly move into. Keyframe and motion prompts follow the same **one-flowing-paragraph, no-word-limit + show-don't-tell** doctrine as Line-by-Line (no labelled fields), with keyframes adding the start-frame rule above plus a safe-zone note since 9:16 platform UI overlays the edges. **Safe framing constrains WHERE the subject sits, not how close the camera is** — held in the middle band (centre, or a third at most) with clear margin all round, nothing at an edge or bleeding off, nothing essential in the top/bottom eighth. That rule has now been wrong in both directions: as "centred with comfortable margins" it turned every keyframe into a centred medium, and rewritten to *encourage* off-centre framing it pushed subjects off the 9:16 edges — so it is stated as two separate axes. Camera position is geometry (lens height, distance, angle), the chest-up-portrait-square-to-the-lens shot stays banned, and the ref-copying fix lives in `buildContinuousPreamble`'s character clause: the ref is a chest-up portrait, declared an identity card whose crop must be ignored. **A motion prompt is simple** (`motionFormat()`): what moves and in which direction, how the camera moves, paced to the clip's real seconds, then one sound direction. It was a three-beat scaffold (departure → crossing → settle) with a `<TRANSITION>` device + anchor riding alongside; both were cut as over-built. One line survives because it prevents a real failure: **write the movement, never a picture of the end frame** — painting the final tableau makes the model race there and freeze. **Motion is seeded per-concept, never auto-relinked:** each of a frame's 3 concepts carries its own `motionPrompt`, and picking a keyframe syncs that concept's motion into the clip (a hand-edit sets `motionEdited` and stops the sync). An automatic **two-image vision pass** used to re-run on every keyframe change, rewriting the motion from the two rendered frames — it was removed: an LLM round-trip per pick made choosing keyframes slow and felt like it was fighting the user. The same pass is still one click away as the clip modal's **Regenerate from frames** (`regenerateContinuousMotion`), which is where it belongs — on demand, for the clip that needs it. `ContinuousScene.motionPrompt` survives only as a fallback seed (the first concept's motion) + legacy support. A storyboard-wide `<STYLE>` block rides OUTSIDE the editable prompts (appended at fire time by `buildContinuousPrompt`) so it can't drift per-frame. Style is picked for BOTH modes from one modal, `StyleModal` (opened by the left panel's single style row; it was a centred popup, then a slide-over back when every other picker slid in, and it is centred again with all of them — September 2026). Its browse view is the **Characters preset picker's grid** — the shared `Modal` at its default 512px, three 9:16 `StyleTile`s across, each a picture of the look under a name plate (built-in presets have no artwork yet and fall back to a palette glyph; a saved style covers itself with its first `thumbRef`). It was a 560px panel of two text cards carrying name + three-line brief, which pushed "Your styles" below the fold; the create view still opens `size='wide'` for the reference-frame editor. It sits on `Modal`'s `layer='below-pickers'` tier so the host's own `BankPicker` — which the create view routes to for reference frames — lands on top of it rather than behind: a **Presets** grid (`CONTINUOUS_STYLES`, whose array order IS the grid order, grouped by family — UGC Realism / 3D Animated / Claymation / Brick Build / Papercraft / Anime / Cartoon. Each one carries **preview art** — `STYLE_PREVIEWS` in `StyleModal.tsx`, keyed by style id, one bundled ~640px JPEG per style under `src/assets/stylePresets/`. They are deliberately **the same scene rendered seven ways**, so the grid is a comparison rather than seven unrelated pictures; regenerate the set together if that scene ever changes. The map lives in the component, not on `ContinuousStyle`, because `visualStyle.ts` is imported by prompt builders and services that have no business pulling bundler asset URLs; a style with no entry falls back to the palette glyph. Brick Build and Cartoon are **described, not branded**: their briefs name the technique (interlocking plastic toy bricks; bean-and-noodle shapes, dark-maroon outline, pastel palette over painted gouache backgrounds) rather than the properties they evoke, so nothing ships a trademark into a member's paid ad. Keep that framing, and keep the `id`s, which key persisted picks. **Cinematic is retired** (July 2026): it's out of the grid but lives on in `RETIRED_STYLES`, which `getContinuousStyle` searches after the live list and before the fallback — a session's `styleId` rides its `brollHistory` row, so dropping the entry outright would relabel every finished Cinematic session "3D Animated" in History and re-render it in the wrong look. Retire a style that way, never by deleting it; `getContinuousStyle`'s fallback is pinned by id, never `[0]`, so reordering can't silently change what an unknown id resolves to) over a **Your styles** grid (the `styles` bank) and a *New from images* card. **Style is picked ON THE STORYBOARD, and nothing waits for it** (August 2026). It was a required row at the bottom of the References card, dashed until picked, with Generate grey until then — which made choosing a look the price of seeing a single shot, in an app where the look is the one decision you actually want to make against frames rather than in the abstract. It costs nothing to move: the style block rides OUTSIDE the editable card prompts and is appended at fire time, so `restyleSession` re-renders the same storyboard in a new look for free. So the style pill on each storyboard (`ScenesView` and `ContinuousView`, both calling the same `onChangeStyle` that opens `StyleModal`) IS the picker — accent-tinted with a chevron, unlike the neutral meta pills beside it — and an unpicked session simply **folds to the mode default**: UGC Realism for Line-by-Line, 3D Animated for Continuous (`resolvedStyleId`; `styleChosen` still gates that fold so a legacy persisted id can't masquerade as a pick). `canGenerate` is the script alone. There is no CLEAR any more — with no empty row to return to, a look is only ever swapped for another — and the picker ticks the resolved style rather than nothing, since the storyboard is already rendering in it. It is NOT desktop-only: it's a control, and a phone's only other route to it is a card's own `StyleNote`. **In `ScenesView` it has since moved up INTO the batch strip** (August 2026) — left end of the bar, cut to the batch buttons' own size, with the scene count that sat beside it dropped, since the storyboard it counts is directly underneath and numbered. **The strip is `h-[57px]`, the app-wide panel-header height, not padding around its pills**: the History rail's band next door is 57px, so `py-3.5` made this one 61px and the two hairlines missed each other by 4px across the seam (Massimo's report, September 2026). **Both strips are ABSOLUTE overlays and the storyboard scrolls UNDER them, blurred** (September 2026, Massimo's call). Three shapes got here and the difference is the point: `sticky` inside the scroller lagged it (see above); a static sibling cured the lag and cost the picture, so glass over it was reverted on sight — a wash over a panel that never moves has nothing to frost and just lifts the band a shade off the panel, reading as a seam. Absolute is both halves: laid out against the panel, so it cannot lag a scroller it is not in, while the scroller runs the full height behind it. The port pays with `pt-[77px]` (the bar's 57px plus the 20px the content already stood off by) — **those two numbers move together**. The fill is **`.app-backdrop-frost` (index.css), which is the PAGE GRADIENT at 90% + `background-attachment: fixed`, over a 30px blur carried on a PSEUDO-ELEMENT — never a flat token, and never with the blur on the same box**, and that distinction is two bug reports. It shipped as `bg-surface-0/72` + `backdrop-saturate-150`: the band read DARKER than the columns either side, and the panel's left divider — a neutral hairline with the page gradient on one side and this band on the other — read as changing colour, "glowing" as saturated cards passed beneath. `index.css` already had the finding written down for the opaque `.app-backdrop-fill` case: a flat token cannot match a radial gradient that moves ~10 units across one bar's width, and 72% of the wrong colour is still the wrong colour. Matched to the gradient the bar is pixel-identical to its surroundings at rest, so the divider reads the same above, inside and below it. The `saturate` went with it (saturating the card underneath is what made the wash read coloured) and the blur settled at **30px** after walking 40 → 20 → 10 → 18 → 30, and the walk is the point: the trip down was really chasing the squeezed gradient below, because a pale band makes any blur look like smear. With the tint painting the right colour the blur can be heavy again, and heavy is what reads as thick glass rather than a dirty window. **The blur is on `::before` because `backdrop-filter` and `background-attachment: fixed` cannot share a box** — the filter makes the element a containing block, and both engines then anchor its own fixed background to its border box, so the whole radial gradient was being squeezed into a 57px band instead of being a 57px window onto a viewport-sized one. That is a different colour by construction: it read as a pale strip, and the neutral hairline down its left edge lost the contrast it has everywhere else on that line, so the divider looked *thinner inside the band than below it*. Split across two boxes it matches again. Alpha is 90%: at rest every value paints the same colour (the tint IS the backdrop), so alpha only ever sets how much a card passing underneath TINTS the bar — it is free to move without any risk of the seam coming back. It went down to 45% and walked back up through 68% and 82%: that low, a bright card stopped tinting the bar and started BEING it. The tint is meant to be felt, not read. A browser with no backdrop-filter gets the opaque gradient instead; a translucent band with no blur over moving stills is unreadable. **Two strips, ONE shape**: Continuous was a centred row that wrapped below `md` ("the buttons got mangled") and is now the Line-by-Line strip in every respect — same 57px, same one-line `w-max min-w-full` scroller, same style-pill-left / actions-right split, same `h-[38px]` pills — which is also what let its `md:` forks on the height and the port's inset go. Its style pill came **up out of the read-only meta caption** to lead that line, where its twin already sat; the caption below the separator is scene count, running time and keyframes picked, and nothing in it is pressable any more. Its lighter generate step is `bg-broll-500/[0.12]`, not the `/[0.05]` it carried — on a 38px pill against a frosted bar that fill read as disabled — and it still sits a clear step under the videos pass. **Every pill on both strips is `h-[38px]`, the stated height `RailNewButton` now carries too**, so the bar and the History rail's New button read level across the seam rather than by coincidence of a font metric. **Download clips is GREEN** — the app's `recommended` emerald, pill and count chip both, `light:` darker in each: the pills beside it GENERATE and this one exports what is already rendered, so a third broll-tinted button read as a third generate pass. **Download clips carries its count in a PILL, not as `(8)` in the label** — parenthesised it read as part of the button's name, and the one number on the row that changes was the least visible thing on it; `tabular-nums` so the pill holds its width as clips land rather than twitching the row's right edge on every completion. A stated height also keeps them level if a pill on either side changes size; the scroll port inside takes `flex-1 min-w-0` rather than a width, which leaves the `-mx-5 px-5` trick intact — with both margins negative the row still resolves to exactly the strip's padding box. `ContinuousView` still carries its pair under the separator. "New" leaves the look alone, exactly as it left the pick alone before. That create view uploads or picks up to 4 frames and runs `analyzeStyleReferences` — a vision pass distilling medium/forms/palette/light/finish into a style paragraph, explicitly forbidden from carrying over the references' subjects. Nothing commits until the user edits/names the result and picks **Use without saving** or **Save to bank & use** (which writes a `StylePreset` row, frames included as `thumbRefs` assets). A custom brief always outranks the preset id; the picked style's `styleBrief` + `styleName` ride the session stamp into `brollHistory` (and `BrollResult.styleName`) so pills read the style's name, not "Custom style". Every style bypasses the app's iPhone-realism stack **except UGC Realism** (`ContinuousResult.realism` → `noRealism: !realism` on both `startImageTask` and `startVideoTask`). Because the style block rides outside the editable card prompts, it's invisible everywhere else — so **every card workspace opens with a `StyleNote`** (shared out of `cardDetailParts`, two-line clamp, tap to expand, **Change** pill opening `StyleModal`: the look rides outside every card prompt, so switching it re-renders the same storyboard in a new style — `restyleSession` in `BrollStudio` rewrites the live result's style stamp and the session stamp, leaving prompts, refs, picks and existing media alone): Continuous' frame + clip modals show `result.style` directly, and the per-line `CardDetailModal` resolves it through `appliedStyleNote`, which mirrors `applyStyleToPrompt` exactly so the note can't promise a look the render won't fire with — a stylized look reads "Style (applied automatically)", UGC Realism and legacy results read "Realism (applied automatically)" with the iPhone stack. Keep the two in step. Worth knowing when a style misses: the **character reference does more of the work than the block** — a photoreal portrait dragged into an anime brief comes back photoreal, so pre-style the character in Characters rather than rewriting the brief. The picker LISTS every standard B-Roll video model (`CONTINUOUS_MODEL_IDS`) with `requireMode='frames-to-video'` greying the non-frame models (Kling Turbo, Omni, Grok) so the user sees why they're unavailable; settings key `broll-studio:continuous:video`, default **Seedance 1.5 Pro** (frames-native and materially cheaper per clip than the 2.0 family — a deliberate cost choice, not a regression; it also backs the standalone-Animate default via its `image-to-video` mode); the picker lives **in the clip modal, not the left panel** — the model only matters once there are keyframes to animate. Layout mirrors Line-by-Line: one serif-numbered row per scene — concept cards + Add-concept in a 4-wide grid, with the `Clip N` card pinned in its own right-hand column (thick purple border marks it as the video card) behind a vertical divider, so added concepts wrap below the images and never under the clip — + a closing **Final Frame** row; each row also carries a per-row **Generate frame** button (mirrors the per-line per-row generate — `requestFrames([idx])`, disabled once that frame's keyframe is picked). **The storyboard is editable in place** (`continuousEdits.ts` — pure ops, applied by `handleEditContinuousStoryboard`): clicking a scene's narration line opens `SceneEditModal` to retype it, **split** it at the cursor (inserting a blank keyframe between the halves), **merge** it with the next scene, or **delete** it. This is also where splitting lives, now that the storyboard never splits a line itself. The frame cards (`${frameIndex}:${conceptId}`), clip cards (`c${sceneIndex}`) and keyframe picks are all keyed by POSITION, so every op declares one old→new index shift and `remapIndexedState` rewrites all three together — never remap one by hand. Affected clips get `motionEdited` cleared so the picked concept's motion re-seeds against the new endpoints. Reordering is deliberately absent: a move invalidates the framing each beat was written to share with its specific neighbours, which is a rewrite, not a move. "Generate frames" renders **every concept of every target frame** in parallel (the batch used to fire `concepts[0]` only, so two of the three cards stayed text and the keyframe was auto-picked before the user saw a choice) — the confirm popup prices images, not frames, and the pick is deterministic (the previously-picked concept, else the leftmost that landed) rather than whichever generation finished first. The per-row button stays live once a frame is picked (it re-arms `includeExisting` and reads **Regenerate frame**) — disabling it was a dead end out of the auto-pick. Falls back to a sequential index-order walk only when some frame in the batch is Chained and has to wait on the one before it, "Generate all clips" fires ready clips in parallel behind a cost-confirm popup (clip count + estimated credits + balance), and **Download clips** (shown once any clip exists) opens `components/ClipDownloadModal` — a tick-list of every rendered clip, pre-ticked to each card's COVER take, zipped via `utils/downloadZip.ts` (jszip). Both modes share it, which is why it lives in `src/components/` rather than here: it takes an `accent`, a `subtitle`, and per-entry `preselected` / `badge` / `meta`, so what's ticked on open is the caller's call. Playground had one too and it came off (August 2026) — a storyboard is a finite set of takes you export together, a Playground history is every clip the member has ever made, and each of those tiles already downloads in one tap. It replaced a "Download all" that swept every take of every card into the zip, so a card regenerated three times landed three clips. **Cover take** = what the card face plays and what its hover Download saves: Line-by-Line sets it from the card modal's gallery (`CardState.selected`), Continuous from the clip modal's take gallery (`currentVideoIndex`) — both badge the picked tile "Cover". Clip cards + gallery tiles keep their hover **Download**. Frame cards open `ContinuousFrameModal` — full per-line image parity: `ModelPicker`, chain/character/product `ReferenceSlotCard`s + `ExtraRefsRow`, Enhance / Regenerate / Undo / Redo (`enhanceContinuousFrame` / `regenerateContinuousFrame` + `frameContextFor`, which feeds the LLM the motions either side so a rewrite still chains), resolution/aspect chips, credits pill, and an image gallery where clicking a tile makes it the keyframe. Clip cards open `ContinuousClipModal` (model picker, endpoint thumbs, motion prompt with **Enhance / Regenerate from frames / Undo / Redo** — Regenerate does the two-image vision pass via `regenerateContinuousMotion`, falling back to start-frame-only until the end keyframe is picked — res/duration/audio, credits chip). Clips inherit their **start keyframe's aspect ratio** (clamped to the video model's list), not a hardcoded 9:16, so 16:9 storyboards animate on the canvas they were drawn for. Sessions ride brollHistory (`continuous*` optional fields) + the same videoHistory/resume machinery; no-key click shows `buildDemoContinuousResult`. Both modes' storyboards sit on the shared `GridCanvas` (grid only while the panel is empty, cleared or generating), and the header's **+** clears the panel back to "Awaiting storyboard", so the last take isn't on camera while the next one is set up. Still no data loss: the session, its cards and its media stay a History row — and the session snapshot never overwrites a row's identity fields (product / character / script / summary) with EMPTY ones, so clearing the inputs can't rename finished work "Untitled session"; swapping one product for another still updates the row. The clear state lives in `BrollStudio` (`clearedCanvasSig`), keyed to the storyboard that was cleared so anything landing DIFFERENT content drops it on its own — but **opening a history row drops it outright**, because re-opening the session you just cleared lands the identical signature and the panel sat on "Awaiting storyboard" with the row loaded behind it until a page refresh. History keeps the plain surface. **Nothing on a card face that fades with hover carries `backdrop-blur`** (August 2026): the three-way shortcut row, the tag chip and the status badges all animate their opacity, and a backdrop-filter re-samples its backdrop every frame it animates — over a real generated still that stuttered visibly on hover, while an empty card looked fine because a flat face is nothing to blur. Solid scrims only, the rule `TileActionStack` already followed. **Every generated still and clip names the model that made it** (`components/ModelPill`, August 2026): Continuous' concept and clip card faces and both `ModalGallery` tiles take the `media` variant — bottom-left over the scrim, `group-hover:opacity-0` on all four, because on every one of those faces the hover row (Use-as-keyframe, Open, the Animate bar) lands on that exact strip. The **Line-by-Line variation card is the exception**: its model rides in the `quiet` variant down in the A-Roll / B-Roll caption UNDER the picture, beside the label it belongs with. On the picture it was a white-on-black chip over the one still the card exists to have judged, and it had to fade on hover anyway — the three-way tab shortcut row lands on that strip. It reads off the entry's own `GeneratedImage.modelId` / `GeneratedVideo.modelId`, so a card whose takes were rendered on different models says so per take rather than reporting whatever the picker is left on; entries persisted before those fields existed simply render no pill. The variation card's stands down entirely for the image-error banner, which owns the same strip and matters more. **History cards deliberately carry none** — a session is not one generation, and a pill there could only name one of the several models a storyboard was built with. Hidden with the rest of the app's generation info by `stores/generationInfoStore.ts` (see the root file).
